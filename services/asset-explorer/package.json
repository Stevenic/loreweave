{
	"name": "@loreweave/asset-explorer",
	"version": "0.1.0",
	"private": true,
	"description": "CLI tool to browse, validate, preview, and generate Pixel Format v1 assets",
	"type": "module",
	"main": "./dist/index.js",
	"bin": {
		"asset-explorer": "./dist/index.js"
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"start": "node dist/index.js",
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "^0.1.0",
		"@loreweave/pixel": "*",
		"@loreweave/types": "*",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"vitest": "^3.1.1"
	}
}
