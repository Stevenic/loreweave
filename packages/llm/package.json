{
	"name": "@loreweave/llm",
	"version": "0.1.0",
	"private": true,
	"description": "LLM provider adapters for narrative generation — supports Anthropic, OpenAI, and OpenAI-compatible APIs",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@loreweave/types": "*"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"vitest": "^3.1.1"
	},
	"files": [
		"dist"
	]
}
