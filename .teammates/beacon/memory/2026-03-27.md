# Beacon — 2026-03-27

## Task 1: Engine Gap Analysis — Bard & Loremaster Content Review

- Reviewed all world content (67 location archetypes, 37+ NPC archetypes, 4 vocabulary tables, Threadhallow world bible, factions, quests, Act I NPCs, Thornwick region)
- Reviewed all current engine code (types, rules, world, narrative — 14 source files)
- Identified 15 engine systems needed to support defined game mechanics
- Categorized into three tiers: foundational (blocks everything), core gameplay (blocks Act I), and advanced (needed later)
- No code changes — analysis/planning only
- Key finding: engine is ~70% complete for basic game loop but missing critical data-driven systems (archetype loading, NPC instantiation, ward mechanics, ceremony system, faction reputation)

## Task 2: Threadhallow WorldConfig Data Files

- Created `worlds/threadhallow/config/` directory with 8 JSON config files
- All data transcribed directly from Loremaster's Threadhallow Mechanics Companion
- **world.json** — id, name, description
- **persona.json** — "The Weave-Keeper" DM persona with tone guide, naming conventions, forbidden topics, special instructions, response style
- **stat-tiers.json** — 5 NPC stat tiers (commoner→elite) with CR/HP/AC ranges, proficiency bonuses, 3 ability arrays each (physical/balanced/mental)
- **ward.json** — 6 ward strength thresholds (0→100) with CR gates and Threadcraft DC modifiers; 11 ward events with strength deltas and Hollowing multipliers
- **fray.json** — 6 exposure levels (Touched→Sighted) with mechanical consequences; 5 exposure sources with save DCs; transformation at level 7 (DC 18)
- **encounters.json** — 3 d20 encounter tables (Forest Stable levels 1-4, Forest Thinning levels 1-4, Frayed Zone levels 5-8); 7 dungeon type modifiers
- **creatures.json** — 6 Fray creature stat blocks: Looseling (CR 1/4), Thread-Puppet (CR 1), Thread-Eater (CR 2), Frayed One (CR 2), Hollow Walker (CR 4), The Unspun (CR 5)
- **skill-challenges.json** — Binding Song ceremony: 6 successes / 3 failures, 7 applicable skills with DC modifiers, 3 failure consequences, critical success bonus, 4-tier base DC table
- All 8 workspaces build clean, all JSON validates
- Key: `loadWorldConfig('worlds', 'threadhallow')` now returns a complete WorldConfig object
- Key: Previous tasks already built all engine code (rules modules, loaders, types, DM wiring) — this task filled in the data layer
- Files created: 8 files in `worlds/threadhallow/config/`

## Task 3: Loremaster D&D Accuracy Fixes (6 Issues)

- Fixed 6 D&D rules accuracy issues identified by Loremaster's review
- **encounter.ts** — Extended CR-to-XP lookup table from CR 8 to CR 30 (official 2024 Free Rules values). Old linear approximation was off by 10-60% at higher CRs.
- **fray.ts** — Added `proficientInConSave` and `profBonus` params to `frayExposureSave()`. D&D saves include proficiency bonus when proficient.
- **skill-challenge.ts** — Natural 20 now counts as 2 successes per Mechanics Companion §7
- **npc-generator.ts** — Archetype-level `primaryAbility` and `primaryArrayType` overrides now take precedence over category defaults. Fixes authority NPCs like Magistrate getting STR instead of INT/CHA.
- **game.ts** — Added `proficientSaves?: AbilityName[]` to `Character`; added `reactions?`, `legendaryActions?`, `lairActions?` to `CreatureStatBlock`; added `'legendary'` to `StatsTier`; added `primaryAbility?` and `primaryArrayType?` to `NpcArchetype`
- **stat-tiers.json** — Split elite tier (CR 9-20) into elite (CR 9-12, prof +4) and legendary (CR 13-20, prof +5) to match D&D proficiency progression
- All 8 workspaces build clean
- Files changed: `packages/rules/src/encounter.ts`, `packages/rules/src/fray.ts`, `packages/rules/src/skill-challenge.ts`, `packages/rules/src/npc-generator.ts`, `packages/types/src/game.ts`, `worlds/threadhallow/config/stat-tiers.json`

## Task 4: Sync LocationArchetype Type with Bard's Schema Update

- Bard added `saving_throws` field to `archetype_format.md` and all 22 dungeon/wilderness location JSONs
- Updated `LocationArchetype.challenges` type in `packages/types/src/game.ts` to include `saving_throws: string[]`
- This separates D&D saving throws from skill checks — different resolution paths (d20 + ability mod + proficiency vs. d20 + skill mod)
- Naming convention: saves use `ability_save_description` format (e.g., `wisdom_save_resist_enchantment`), skill checks use `skill_action` format (e.g., `perception_spot_tracks`)
- All 8 workspaces build clean
- Files changed: `packages/types/src/game.ts`

## Task 5: Structure Placement (Gap Analysis #9)

- Implemented deterministic grid-based structure placement — chunk pipeline Stage 5 is no longer a no-op
- Created `packages/world/src/structures.ts` with two main functions:
  - `computeStructurePlacements()` — grid hash per structure type, positional RNG per cell, biome/elevation validation
  - `applyStructuresToTiles()` — modifies tiles within footprint: surfaces, features, walkability
- 5 structure types placed: village (spacing 128, freq 35%), ruins (96, 25%), cave_entrance (64, 30%), shrine (48, 20%), watchtower (80, 15%)
- Bridge placement deferred — requires river-aware logic
- Villages get `settlementTier` (hamlet/village/town/city) and `settlementId` for ward tracking
- Settlement tier distribution: ~50% hamlet, ~30% village, ~15% town, ~5% city
- Village tile mods: stone center square (r≤2), dirt paths (r≤60% radius), partial clearing (outer ring)
- Ruins: scattered stone + debris. Cave entrance: stone platform. Shrine: small stone pad. Watchtower: cleared trees for LOS.
- Extended `StructureRef` type with `radius`, `settlementTier?`, `settlementId?`
- Verified: deterministic output, reasonable distribution (2 villages, 5 ruins, 4 caves, 9 shrines in 11×11 chunks), correct tile modifications
- All 8 workspaces build clean
- Files created: `packages/world/src/structures.ts`
- Files changed: `packages/types/src/game.ts`, `packages/world/src/chunk-generator.ts`, `packages/world/src/index.ts`
- Key: grid hashing eliminates multi-chunk coordination — each cell's output depends only on (seed, cellX, cellY)
- Key: MAX_STRUCTURE_RADIUS bounds the grid cell search, keeping it O(constant) per chunk
