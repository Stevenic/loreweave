---
compressed: true
---

# Beacon — 2026-03-26

## Task 1: Build Pixel Resource Explorer
- Built `@loreweave/pixel` package (decoder, validator, loader, terminal renderer) + `@loreweave/resource-explorer` CLI (5 commands).
- Created 6 sample pixel assets. Fixed root build script to `tsc --build`.
- Key: explorer is a service, not part of pixel package.

## Task 2: Implement Full Pixel Format v1
- Rewrote pixel type system to match spec §13 (all 6 file types, 30+ types). Built 8 modules: decoder, color, animation, emitter, validator, canvas-renderer, terminal-renderer, loader.
- Key: CanvasFactory injection (no browser globals). TS6 `Object.entries` returns `unknown` — needs casts.

## Task 3: Progressive Palette System
- Created fantasy16 → fantasy32 → fantasy64 → fantasy72 strict superset chain. 72 is max indexed palette per spec §4.2.
- Files created: 3 palette files in `assets/palettes/`.

## Task 4: Palette Depth Remapping
- Added `colorDistance()`, `remapPalette()` to color.ts. Render-time palette remapping via `targetPalette` option.
- Files changed: `packages/pixel/src/color.ts`, `canvas-renderer.ts`, `index.ts`

## Task 5: Backlog — Dynamic Lighting Effects
- Created backlog for firelight/lighting. Current workaround needs manual glow masks. Gaps: per-item tint, color tracks, declarative lights.
- Filed at: `.teammates/beacon/memory/backlog_dynamic_lighting.md`

## Task 6: WebGL & Eleven Labs Assessment
- Canvas 2D sufficient for tint; WebGL needed for per-pixel radial falloff. Added lip sync/sound section to backlog.

## Task 7: Agent SDK Research
- Researched Claude Agent SDK. Architecture: agent + MCP tools for validation/preview. Recommended rename to asset-explorer.

## Task 8: Asset Explorer Rename + Agent SDK Generate Command
- Renamed resource-explorer → asset-explorer. Added Agent SDK + zod deps. Built 3 MCP tools (validate, preview, list) + `generate` CLI command.
- Key: lazy-import for Agent SDK so non-generate commands work without it.

## Task 9: Wisdom Distillation
- Added 5 new WISDOM entries: tsc --build ordering, TS6 Object.entries, CanvasFactory injection, lazy-import, seeded PRNG.

## Task 10: PPU Implementation
- Added `ppu` to sprite/tileset/emitter types, `referencePpu` to scene canvas. Validator + renderer updated. Defaults to 32 per spec.
- Files changed: `pixel.ts`, `pixel-schema.json`, `pixel-schema.llmd`, `validator.ts`, `canvas-renderer.ts`

## Task 11: Rename Asset Explorer → Pixel Explorer
- Renamed asset-explorer → pixel-explorer. Reoriented as generation-first tool.

## Task 12: Wisdom Distillation
- Fixed stale reference. Added "Agent SDK with in-process MCP tools" entry.

## Task 13: Pixel Explorer → Web Server
- Rewrote from CLI to local web server (node:http + WebSocket + file watcher). REST API: assets, validate, palettes, generate. Dark-themed web UI with gallery, validation, generation.
- Files: `server.ts` (NEW), `html.ts` (NEW), `index.ts`, `package.json`. Version 0.2.0.

## Task 14: SPA with Sidebar Navigation
- Rewrote to hash-based SPA: Gallery, Palettes, Validate, Generate, Detail views. WebSocket cache invalidation.

## Task 15: Fix Generate Exit Code 11
- Agent SDK on Windows triggers interactive Credential Manager → fails headless. Added ANTHROPIC_API_KEY env var check.

## Task 16: .env File Support
- Zero-dependency .env loader on startup. Env vars take precedence over .env file.

## Task 17: Configurable Model + example.env
- Added CLAUDE_MODEL env var + model selector dropdown. Created `example.env`.

## Task 18: Debug Generate Exit Code 1
- Added stderr callback + env propagation + error result handling to Agent SDK query.

## Task 19: Fix Build + npx Resolution
- Root cause: stale .tsbuildinfo/.js/.d.ts artifacts tricked tsc --build into no-op. Added `pixel-explorer` script to root package.json.

## Task 20: Handle EADDRINUSE
- Added server error handler for port-in-use with helpful message.

## Task 21: Fix Marketplace Config Error
- Empty `known_marketplaces.json` crashed Agent SDK. Fixed to `[]`. Upgraded SDK to 0.2.84, zod to 4.3.6.

## Task 22: Fix Missing dist/ (Again)
- Same stale artifact root cause as Task 19. Cleaned and rebuilt.

## Task 23: Fix Root Cause of Stale .js in src/
- Root tsconfig had no outDir/files restriction — accidental `tsc` drops .js next to .ts. Fixed with `"outDir": "./dist"` + `"files": []`. Added clean/rebuild scripts.

## Task 24: --debug Flag
- Added `--debug` for verbose Agent SDK logging: turns, tools, tokens, cost, errors. Color-coded ANSI output.

## Task 25: Fix Generate Hang
- `acceptEdits` permission mode hangs headless. Switched to `bypassPermissions`. Added 5-min AbortController timeout.

## Task 26: Remove MCP Tools (Deadlock Fix)
- In-process MCP tools deadlock on Windows (stdio pipe contention). Removed all MCP tools; agent uses only built-in Write/Read/Glob. Server-side post-generation validation instead.
- Key: MCP tools can return later via external stdio server process.

## Task 27: Replace Agent SDK with @loreweave/agents
- Agent SDK abandoned due to Windows deadlocks. Created `@loreweave/agents` package: `AgentAdapter` interface, `CliProxyAdapter` (spawns CLIs), presets for Claude/Codex/Aider.
- Key: same `execute()` + `AgentResult` interface for future LLM API adapters.
- Files created: 5 files in `packages/agents/src/`. Files changed: pixel-explorer server, package.json files.

## Task 28: Fix External Palette References
- Client-side renderer only handled inline palettes, not string refs like `"palette": "fantasy64"`. Added palette preload cache.

## Task 29: Fix Client-Side RLE Decoder
- Client decoder parsed char-by-char instead of whitespace-split. Rewrote to match server-side logic.

## Task 30: Fix Regex in Template Literal
- Actual root cause of Tasks 28-30: `\s`, `\d` in regex inside template literals lose backslashes. Fix: double-escape (`\\s`, `\\d`).
- Key: ALL regex special classes in JS template literals need double-escaping.

## Task 31: Animation Playback Controls
- Client-side animation engine with play/pause, loop, clip selector, seekable progress bar, 60fps via requestAnimationFrame.

## Task 32: Move Generation to @loreweave/pixel
- Extracted `generatePixelAsset()` from pixel-explorer into `packages/pixel/src/generator.ts`. Accepts any `AgentAdapter` via DI.
- Supports both file-based and pre-loaded context for design-time vs runtime use.

## Task 33: Auto-Sizing System
- Created `packages/pixel/src/sizing.ts`: 12 sprite archetypes, 3 detail levels (PPU 16/32/64), keyword-based archetype inference.
- Generator auto-infers archetype from prompt, injects sizing into system prompt. Web UI shows live sizing hint.
- Key: user never specifies dimensions — just picks detail level.

## Task 34: Asset Manager Restructure
- Restructured from flat files to managed assets: folder-per-asset with `asset.json` metadata + named views.
- New types: `AssetMeta`, `AssetView`, `AssetReference`. Migration module auto-converts existing files at startup.
- REST API updated: managed/unmanaged split, create/update endpoints. Generation now asset-scoped.
- Key: references enable composition (fire + smoke as components of campfire).

## Task 35: Full CRUD for Assets and Views
- Added delete asset, delete view, rename view to loader + server + web UI with confirmation dialogs.

## Task 36: Fix Duplicate Palettes + IndexedDB Prefs
- Palette dropdown race condition fixed via synchronous cache population. Added IndexedDB persistence for generate form preferences (detail level, palette, model).

## Task 37: Custom Colors + Parallel Generation
- Custom colors per asset (max 8, merged on top of palette). Parallel generation via 202 Accepted + WebSocket status events + gallery spinners.
- Key: custom colors are asset-level, not view-level.

## Task 38: Game Engine Foundation
- Built three core packages from stubs: `@loreweave/types` (60+ game types), `@loreweave/rules` (dice, abilities, combat), `@loreweave/world` (rng, noise, biomes, surface, resources, features, spawns, chunk-generator, world).
- Key: OpenSimplex2 noise, all pure functions, structure placement is no-op placeholder.

## Task 39: DungeonMaster — Narrative Engine
- Built full `@loreweave/narrative`: intent-parser (keyword→GameAction), context-assembler, action-resolver (10 action types), prompt-builder, DungeonMaster class.
- Architecture: Player Input → Intent Parser → Rules Engine → World State → LLM (narrative only). LLM never decides mechanics.
- Key: `WorldAccess` interface prevents circular deps. `NarrativeAdapter` is pluggable.
