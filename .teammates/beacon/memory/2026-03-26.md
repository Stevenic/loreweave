# Beacon — 2026-03-26

## Task 1: Build Pixel Resource Explorer

- Implemented full `@loreweave/types` pixel type system matching spec sections 15-16
- Built `@loreweave/pixel` package: decoder (raw + RLE), validator, loader, terminal renderer
- Created `@loreweave/resource-explorer` CLI service with 5 commands: list, info, validate, preview, palette
- Created 6 sample pixel assets: fantasy16 palette, campfire/pine_tree/torch sprites, forest_ground tileset (4 tiles), forest_clearing scene
- Fixed root build script: changed from `npm run build --workspaces` to `tsc --build` for correct project reference ordering
- Added `@types/node` to root devDeps and `types: ["node"]` to base tsconfig
- All 7 workspaces build clean, lint passes, tests pass
- Key decision: explorer is a service (not part of pixel package) for clean separation

## Task 2: Implement Full Pixel Format v1

- Rewrote `@loreweave/types` pixel.ts to match rewritten spec §13 — all 6 file types, 30+ types
- Built 8 modules in `@loreweave/pixel`:
  - `decoder.ts` — raw + RLE decoding, layer compositing, frame extraction
  - `color.ts` (NEW) — color parsing, RGBA conversion, alpha blending
  - `animation.ts` (NEW) — easing functions (step/linear/ease-in/out), keyframe interpolation, clip playback (loop/once/pingpong)
  - `emitter.ts` (NEW) — seeded PRNG particle system (xorshift32), physics sim, burst + continuous modes
  - `validator.ts` — full rewrite: validates all 6 file types with warnings support
  - `canvas-renderer.ts` (NEW) — Canvas 2D pipeline: sprite rasterization, tile rendering, scene compositing with blend modes, parallax, camera, y-sort, attachments, emitters, shadows
  - `terminal-renderer.ts` (renamed from renderer.ts) — updated for layers/frames/new palette type
  - `loader.ts` — added tilemap + emitter file type support
- Updated resource explorer for all new APIs (6 file types, tileset registries, warnings)
- Updated all 6 sample assets: palette shape changed from flat `Record<string, PixelColor>` to `{ entries: {...} }` per new spec
- Enhanced fantasy16 palette with aliases and ramps from spec §4.3-4.4
- All 7 workspaces build clean, lint passes (warnings only), tests pass, resource explorer validates all 6 assets
- Key: Canvas renderer accepts `CanvasFactory` injection — no browser globals, works anywhere
- Key: TypeScript 6 strict `Object.entries` returns `unknown` values — requires explicit casts

## Task 3: Progressive Palette System

- Created 3 new palettes forming a strict superset chain: fantasy16 → fantasy32 → fantasy64 → fantasy72
- Each tier adds colors while preserving all keys/values from the previous tier (verified programmatically)
- fantasy32 (+16): blues, purples, skin tones, orange, teal, white, void
- fantasy64 (+32): coral, pink, olive, pastels, lavender, sea, magenta, rust, night, parchment, slate
- fantasy72 (+8): fills remaining key space with mint, frost, steel-blue, rose, sky, apricot, deep-forest
- 72 is the max indexed palette (spec §4.2 allows 72 single-char keys)
- All palettes validate clean via resource explorer
- All ramps updated at each tier to include new intermediate colors
- Files created: `assets/palettes/fantasy32.palette.pixel.json`, `fantasy64.palette.pixel.json`, `fantasy72.palette.pixel.json`

## Task 4: Palette Depth Remapping

- Added `colorDistance()`, `remapPalette()`, `remapPaletteEntries()` to `color.ts`
- Wired `targetPalette` option into `renderScene()` and `renderSprite()` in canvas-renderer
- Remapping flows through tilemap layers, item layers, and individual scene items
- Uses Euclidean RGB distance for nearest-color matching
- Remapping is render-time only — .pixel files always store authored palette
- All 7 workspaces build clean
- Files changed: `packages/pixel/src/color.ts`, `packages/pixel/src/canvas-renderer.ts`, `packages/pixel/src/index.ts`

## Task 5: Backlog — Dynamic Lighting Effects

- Created backlog task for dynamic firelight/lighting effects on character faces
- Documented current capabilities (blend modes, animated opacity, emitters) and gaps (per-item tint, color tracks, declarative lights, radial falloff)
- Filed at: `.teammates/beacon/memory/backlog_dynamic_lighting.md`
- No code changes — planning/documentation only

## Task 6: WebGL & Eleven Labs Assessment (Backlog Update)

- Updated dynamic lighting backlog with WebGL upgrade path analysis
- Canvas 2D sufficient for tint + color tracks; WebGL needed for per-pixel radial light falloff and multiple dynamic lights
- Added future lip sync + sound effect section: Eleven Labs phoneme timing → viseme sprite frames, Web Audio API for spatial sound
- Lip sync is cross-team: spec (@scribe), prompt engineering (@lexicon), renderer (beacon)
- Files changed: `.teammates/beacon/memory/backlog_dynamic_lighting.md`

## Task 7: Agent SDK Research & Asset Explorer Rename Assessment

- Researched Claude Agent SDK (formerly Claude Code SDK) — `@anthropic-ai/claude-agent-sdk`
- The SDK exposes Claude Code as a programmable library via `query()` function
- Key capabilities: built-in tools (Read/Write/Edit/Bash/Glob/Grep), custom MCP tools via `createSdkMcpServer()` + `tool()`, subagents, hooks, sessions, streaming
- Architecture for pixel asset generation: Agent SDK agent with pixel format system prompt + custom MCP tools for validation/preview
- Assessed rename from `resource-explorer` → `asset-explorer` — recommended yes, broader scope fits generation + browsing
- No code changes — research/planning only

## Task 8: Asset Explorer Rename + Agent SDK Generate Command

- Renamed `@loreweave/resource-explorer` → `@loreweave/asset-explorer`
  - Moved `services/resource-explorer/` → `services/asset-explorer/`
  - Updated package.json name, bin, description
  - Updated root build script path
  - Updated all CLI help text and usage references
- Added `@anthropic-ai/claude-agent-sdk` (v0.1.77) and `zod` (v3.24) as dependencies
- Built 3 custom MCP tools for pixel asset generation:
  - `validate_pixel` — validates pixel JSON against spec using our existing validators
  - `preview_pixel` — renders sprite JSON to ANSI terminal string for visual verification
  - `list_assets` — scans asset directory, returns file paths and types
- Added `generate` command to CLI:
  - `asset-explorer generate "prompt" [--type sprite|tileset|tilemap|scene] [--palette <file>] [--output <dir>]`
  - Lazy-imports Agent SDK so non-generate commands work without it
  - Loads `pixel_format_llm_guide.md` and `pixel-schema.llmd` as system prompt context
  - Defaults to fantasy32 palette if no palette specified
  - MCP tools registered via `createSdkMcpServer()` — in-process, no external server
  - Agent gets Write/Read/Glob + all 3 pixel MCP tools, runs in `acceptEdits` permission mode
  - Streams tool call names and final result to terminal
- All 7 workspaces build clean, lint passes, tests pass, all 9 assets validate
- Key: installed Agent SDK v0.1.77 `tool()` takes 4 args (no annotations param yet — docs show 5 but SDK hasn't shipped it)
- Key: Dynamic import for Agent SDK + Zod means the CLI degrades gracefully if they're not installed

## Task 9: Wisdom Distillation

- Updated WISDOM.md: bumped compaction date to 2026-03-26
- Added 5 new entries from today's work:
  - `tsc --build` for monorepo build ordering (from Task 1)
  - TS6 `Object.entries` strict behavior (from Task 2)
  - Factory injection pattern — CanvasFactory (from Task 2)
  - Lazy-import optional heavy dependencies (from Task 8)
  - Seeded PRNG for deterministic particle effects (from Task 2)
- All 7 existing entries retained — still accurate
- Files changed: `.teammates/beacon/WISDOM.md`

## Task 10: PPU (Pixels Per Unit) Implementation

- Added `ppu?: number` to `PixelSprite`, `PixelTileset`, `EmitterConfig` types in `packages/types/src/pixel.ts`
- Added `referencePpu?: number` to `PixelScene.canvas` type
- Updated `pixel-schema.json`: added `ppu` to PixelSprite, PixelTileset, EmitterConfig, PixelEmitter; added `referencePpu` to PixelScene canvas
- Added validation rules in `packages/pixel/src/validator.ts`: ppu must be positive integer on sprites, tilesets, emitters; referencePpu must be positive integer on scene canvas
- Updated `packages/pixel/src/canvas-renderer.ts`:
  - `renderSceneItem` computes `ppuFactor = sprite.ppu / canvas.referencePpu` per §11.5 scale chain
  - `renderEmitterParticles` scales particle positions and sizes by emitter ppuFactor per §11.6
  - Threaded `referencePpu` through `renderItemsLayer` → `renderSceneItem` → `renderEmitterParticles`
- Updated `pixel-schema.llmd` with new ppu/referencePpu properties
- All 7 workspaces build clean, lint passes, tests pass
- Key: ppuFactor defaults to 1.0 when `referencePpu` is not set (backward compatible)
- Key: existing assets without `ppu` default to 32 per spec
- Files changed: `packages/types/src/pixel.ts`, `pixel-schema.json`, `pixel-schema.llmd`, `packages/pixel/src/validator.ts`, `packages/pixel/src/canvas-renderer.ts`

## Task 11: Rename Asset Explorer → Pixel Explorer

- Renamed `@loreweave/asset-explorer` → `@loreweave/pixel-explorer`
  - Moved `services/asset-explorer/` → `services/pixel-explorer/`
  - Updated package.json: name, bin (`pixel-explorer`), description (generation-first)
  - Updated root build script path
  - Replaced all `asset-explorer` CLI text references with `pixel-explorer`
- Reoriented help text to lead with `generate` command
  - Split COMMANDS into GENERATE and BROWSE & INSPECT sections
  - Generation examples listed first
  - Description changed from "Asset Explorer & Generator" to "Asset Generator & Explorer"
- All 7 workspaces build clean, tests pass
- Files changed: `services/pixel-explorer/package.json`, `services/pixel-explorer/src/index.ts`, `package.json`

## Task 12: Wisdom Distillation

- Fixed stale "asset-explorer" → "pixel-explorer" reference in lazy-import entry
- Added new entry: "Agent SDK with in-process MCP tools for generation" — durable architectural pattern from Task 8
- All 12 entries reviewed, all still accurate — no removals
- Files changed: `.teammates/beacon/WISDOM.md`

## Task 13: Pixel Explorer → Web Server

- Rewrote `@loreweave/pixel-explorer` from CLI tool to local web server
- **Architecture**: `pixel-explorer start [--port 3000] [--dir ./assets]`
  - `src/index.ts` — CLI entry point, directory scaffolding, server startup
  - `src/server.ts` — HTTP server (node:http), REST API, WebSocket (ws), file watcher (node:fs.watch)
  - `src/html.ts` — Full web UI as template strings (HTML/CSS/JS, no build step for frontend)
- **REST API**:
  - `GET /api/assets` — list all assets grouped by type
  - `GET /api/asset?path=...` — get single asset JSON + metadata
  - `GET /api/validate` — validate all assets, return results with summary
  - `GET /api/palettes` — list available palettes
  - `POST /api/generate` — generate asset via Agent SDK (lazy-imported)
- **Web UI**:
  - Dark theme (GitHub-style) with asset gallery grid
  - Client-side pixel renderer (raw + RLE decoding, canvas rendering)
  - Sprite and tileset canvas previews with auto-scaling
  - Palette swatch display
  - Type filter sidebar with stats
  - Detail modal with full JSON view + rendered preview
  - Validation modal with pass/fail/warning per asset
  - Generation modal with prompt/type/palette inputs
  - WebSocket auto-reconnect for live reload on file changes
- **Dependencies added**: `ws` ^8.18.0, `@types/ws` ^8.18.0
- **Security**: Path traversal check on asset API endpoint
- Removed old CLI commands (list, info, validate, preview, palette) — all now in web UI
- All 7 workspaces build clean, lint passes, tests pass
- Version bumped to 0.2.0
- Files changed: `services/pixel-explorer/src/index.ts`, `services/pixel-explorer/src/server.ts` (NEW), `services/pixel-explorer/src/html.ts` (NEW), `services/pixel-explorer/package.json`

## Task 14: Pixel Explorer Web UI → SPA with Sidebar Navigation

- Rewrote `html.ts` from modal-based UI to full single-page app with sidebar navigation
- **Views**: Gallery (#gallery), Palettes (#palettes), Validate (#validate), Generate (#generate), Detail (#detail/:path)
- Hash-based routing with browser back button support
- Asset data caching with WebSocket-driven cache invalidation on file changes
- Connection status indicator in sidebar (green/red dot + text)
- **Gallery**: filter tabs by type, card grid with canvas thumbnails, click-through to detail
- **Palette viewer**: swatches with hover tooltips (key, alias, hex), ramp visualization, sorted by entry count (shows superset chain)
- **Validation dashboard**: summary stat cards (total/passed/failed/warnings), expandable error/warning details per asset
- **Generate panel**: prompt textarea, type/palette dropdowns, Agent SDK integration
- **Detail view**: large canvas preview, property grid, full JSON display, back button
- All 7 workspaces build clean, tests pass
- Files changed: `services/pixel-explorer/src/html.ts`

## Task 15: Fix Generate Exit Code 11 (Auth Failure)

- Diagnosed: Agent SDK spawns Claude Code subprocess → Windows tries interactive Credential Manager prompt → fails headless (exit code 11)
- Added `ANTHROPIC_API_KEY` environment variable check before attempting generation
- Returns clear error message if env var is missing, explaining the requirement
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`

## Task 16: Add .env File Support

- Added zero-dependency `.env` file loader to `index.ts` — reads `.env` from cwd on startup
- Supports `KEY=VALUE`, quoted values (single/double), comments (#), blank lines
- Does not overwrite existing environment variables (env vars take precedence over .env)
- Updated generate endpoint error message to mention .env as an option
- Added ENVIRONMENT section to CLI help text
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/index.ts`, `services/pixel-explorer/src/server.ts`

## Task 17: Configurable Model + example.env

- Agent SDK `query()` accepts a `model?: string` option — defaults to CLI default model
- Added `CLAUDE_MODEL` env var support in `apiGenerate()` — priority: request body param > env var > SDK default
- Added model selector dropdown to web UI Generate panel: Sonnet 4.5, Sonnet 4.6, Haiku 4.5, Opus 4.6, or "Default"
- Created `services/pixel-explorer/example.env` with documented `ANTHROPIC_API_KEY` and `CLAUDE_MODEL` options
- Updated CLI help text with `CLAUDE_MODEL` env var and reference to example.env
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/index.ts`, `services/pixel-explorer/src/html.ts`, `services/pixel-explorer/example.env` (NEW)

## Task 18: Debug Generate Exit Code 1

- Exit code 1 from Agent SDK had zero diagnostic information — error message was just "Claude Code process exited with code 1"
- Root cause unknown until we can see stderr — the Agent SDK `query()` options have a `stderr` callback we weren't using
- Added `stderr: (data) => { ... }` callback to capture subprocess error output and log to server console
- Added explicit `env: process.env` to `query()` options to ensure ANTHROPIC_API_KEY propagates to the subprocess
- Now handle error result messages from the query iterator (error_during_execution, error_max_turns, etc.) instead of silently ignoring them
- Web UI shows `details` field from error responses containing stderr output
- Server-side console.error logging for all error paths
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/html.ts`

## Task 19: Fix Build + npx Resolution

- Root cause: stale `.tsbuildinfo` files and stale `.js`/`.d.ts` artifacts in `src/` dirs from previous builds made `tsc --build` think packages were up-to-date while `dist/` was empty
- Cleaned all `.tsbuildinfo`, stale `src/*.js`, `src/*.d.ts` artifacts
- `npm install` was needed to create workspace symlinks in `node_modules/@loreweave/`
- `npm run build` succeeds — all 7 workspaces compile clean
- `npx pixel-explorer` from `services/pixel-explorer/` fails because npx resolves from npm registry, not local workspace
- Added `"pixel-explorer"` script to root `package.json` → `npm run pixel-explorer` works from monorepo root
- Correct invocations: `npm run pixel-explorer` (from root), `npx pixel-explorer` (from root), or `node dist/index.js start` (from service dir)
- Files changed: `package.json` (root)

## Task 20: Handle EADDRINUSE Gracefully

- Port 8080 was in use from a previous server instance — crashed with unhandled 'error' event
- Added `server.on('error')` handler in `startServer()` that catches `EADDRINUSE`
- Prints clear message: which port is busy, suggests stopping the old process or using `--port <next>`
- Exits with code 1 instead of stack trace
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`

## Task 21: Fix "Failed to load marketplace configuration" Error

- Root cause: `~/.claude/plugins/known_marketplaces.json` was a 0-byte empty file
- Agent SDK tried to `JSON.parse()` it during initialization → "Unexpected end of JSON input" → exit code 1
- Fixed by writing valid empty JSON `[]` to the file
- Upgraded Agent SDK from 0.1.77 → 0.2.84 (latest)
- Upgraded zod from 3.x → 4.3.6 (required by SDK 0.2.84 peer dep)
- `tool()` and `query()` APIs backward-compatible — no code changes needed
- All 7 workspaces build clean with new deps
- Files changed: `~/.claude/plugins/known_marketplaces.json` (fixed), `services/pixel-explorer/package.json` (deps bumped)

## Task 22: Fix Missing dist/ — Clean Rebuild (Again)

- `npx pixel-explorer` failed with MODULE_NOT_FOUND — `dist/` was empty
- Same root cause as Task 19: stale `.js`/`.d.ts`/`.tsbuildinfo` in `src/` dirs tricked `tsc --build` into no-op
- Cleaned all stale artifacts across all workspaces, rebuilt — all 7 workspaces compile clean
- No source file changes — build artifacts only

## Task 23: Fix Root Cause of Stale .js in src/

- Root cause identified: root `tsconfig.json` had no `outDir` and no `files`/`include` restriction
- If `tsc` is ever invoked against the root config directly (IDE auto-compile, accidental `tsc` run), it compiles ALL `.ts` files in the tree and drops `.js` next to them — TypeScript's default when no `outDir` is set
- `tsc --build` was fine (it follows workspace references), but any other invocation using root config caused pollution
- Fix: added `"outDir": "./dist"` as safety net + `"files": []` to prevent root config from being directly compilable
- Also added proper `clean` and `rebuild` scripts to root `package.json`:
  - `clean`: `tsc --build --clean` (removes only tsc-generated files, respects workspace configs)
  - `rebuild`: `npm run clean && tsc --build --force` (guaranteed clean slate)
- All 7 workspaces build clean after `tsc --build --force`
- Files changed: `tsconfig.json`, `package.json`

## Task 24: Add --debug Flag for Agent SDK Logging

- Added `--debug` CLI flag to `pixel-explorer start`
- When enabled, logs every Agent SDK message to the server console:
  - **assistant** turns: model name, input/output token counts, each content block (tool_use name + truncated input, text blocks)
  - **tool_use_summary**: tool result summaries
  - **tool_progress**: tool name + elapsed time
  - **result**: success/error status, turn count, duration (total + API), cost in USD, per-model token breakdown
  - **system**: init, api_retry (with status + delay), compact_boundary, etc.
  - **auth_status**: authenticating state + errors
- Color-coded ANSI output: cyan prefix, yellow for tools/retries, green for success, red for errors, dim for metadata
- Truncates tool inputs to 200 chars and text blocks to 300 chars for readability
- Also logs generation params (prompt, type, model, output dir) at start
- stderr callback respects debug mode formatting
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/index.ts`, `services/pixel-explorer/src/server.ts`

## Task 25: Fix Generate Hang After Init

- Generation was stuck after `system: init` debug message — no further Agent SDK messages yielded
- Root cause likely: `permissionMode: 'acceptEdits'` still prompts interactively for MCP tool approval, causing headless subprocess to hang
- Switched to `permissionMode: 'bypassPermissions'` + `allowDangerouslySkipPermissions: true`
- Added AbortController with 5-minute timeout — generation can no longer hang forever
- Added "first model response received" debug log between init and first API response
- Timeout produces clear error message instead of cryptic exception
- Key: `bypassPermissions` is appropriate here — this is a local dev tool, not production
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`

## Task 26: Fix Generate Stuck — Remove MCP Tools

- Root cause: in-process MCP tools (`createSdkMcpServer()`) communicate with Agent SDK subprocess via bidirectional stdio pipes — causes deadlock on Windows
- Evidence: `(in: 3, out: 0)` debug output = only 3 input tokens, subprocess deadlocked during MCP handshake before API call
- Fix: removed all MCP tools from `query()` call — no `mcpServers`, no `createSdkMcpServer()`, no zod dependency
- Agent now uses only built-in tools (Write, Read, Glob) to generate and write files
- Added server-side post-generation validation: finds newly written file, validates with existing validators, includes result in API response
- Removed unused imports (`renderSpriteToTerminalString`, zod lazy-import)
- System prompt updated: no MCP tool references, clearer file-writing instructions
- All 7 workspaces build clean
- Files changed: `services/pixel-explorer/src/server.ts`
- Key: MCP tools can be re-added later using external stdio MCP server process instead of in-process transport

## Task 27: Replace Agent SDK with @loreweave/agents Package

- Agent SDK (`@anthropic-ai/claude-agent-sdk`) kept timing out / deadlocking on Windows — abandoned in favor of direct CLI spawning
- Created new `@loreweave/agents` package at `packages/agents/` with:
  - `AgentAdapter` interface — core abstraction supporting both coding agents (CLI spawn) and future LLM API adapters
  - `CliProxyAdapter` — spawns coding agent CLIs as child processes (ported from @teammates CLI pattern)
  - `AgentPreset` system — preset definitions for Claude, Codex, Aider CLIs
  - `ExecuteOptions` / `AgentResult` / `ProgressEvent` types
- Claude preset: spawns `claude -p --verbose --dangerously-skip-permissions`, prompt via stdin, Windows shell mode
- Codex preset: spawns `codex exec - --full-auto --ephemeral --json`, JSONL output parsing
- Aider preset: spawns `aider --message-file <tempfile> --yes --no-git`
- Adapter handles: temp file management, Windows .cmd shell mode, timeout with SIGTERM→SIGKILL escalation, abort signal, changed file detection from output, env var propagation
- Updated `@loreweave/pixel-explorer`:
  - Replaced Agent SDK `query()` with `CliProxyAdapter` using `claude` preset
  - Removed `@anthropic-ai/claude-agent-sdk` and `zod` dependencies
  - Added `@loreweave/agents` dependency
  - Simplified generate endpoint: ~50 lines vs ~150 lines
  - Debug logging uses ProgressEvent callbacks instead of SDK message parsing
  - Server-side validation still runs after generation
- Updated root build script: agents package added between rules and pixel in dependency order
- All 8 workspaces build clean, tests pass, lint passes
- Files created: `packages/agents/package.json`, `packages/agents/tsconfig.json`, `packages/agents/src/types.ts`, `packages/agents/src/adapter.ts`, `packages/agents/src/presets.ts`, `packages/agents/src/cli-proxy-adapter.ts`, `packages/agents/src/index.ts`
- Files changed: `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/package.json`, `services/pixel-explorer/tsconfig.json`, `package.json`
- Key: interface designed for future LLM API adapters (Anthropic Messages API, OpenAI) — same `execute()` signature, same `AgentResult`

## Task 28: Fix Canvas Rendering for External Palette References

- Root cause: `resolvePaletteEntries()` in client-side JS only handled inline palette objects (`data.palette.entries`), not string references like `"palette": "fantasy64"`
- Generated campfire sprite uses `"palette": "fantasy64"` (string reference to external palette file) → renderer returned null → fell through to placeholder
- Added `paletteEntriesCache` object to client state — maps palette names to their entries
- Added `preloadPalettes()` function: fetches `/api/palettes` list, then each palette's full data via `/api/asset`, caches entries by name
- Updated `resolvePaletteEntries()` to check cache when `data.palette` is a string
- App init now awaits `preloadPalettes()` before first `onHashChange()` so gallery thumbnails have palette data available
- All 8 workspaces build clean
- Files changed: `services/pixel-explorer/src/html.ts`

## Task 29: Fix Client-Side RLE Decoder

- Root cause: client-side `decodeRleRow()` parsed character-by-character instead of splitting by whitespace first
- The RLE format uses space-delimited tokens like `"640 24 620"` where each token is `<count><key>` (e.g., "640" = 64 repetitions of palette key '0')
- Old decoder saw "640" as numStr="640" then used the space as the palette key — completely wrong parsing
- Server-side decoder (`packages/pixel/src/decoder.ts`) correctly splits by whitespace, then regex matches `^(\d+)(.)$` per token
- Rewrote client-side decoder to match: `row.trim().split(/\s+/)` then regex match each token
- All 8 workspaces build clean
- Files changed: `services/pixel-explorer/src/html.ts`

## Task 30: Fix Regex Escaping in Template Literal — RLE Still Broken

- Root cause: `decodeRleRow()` JavaScript code lives inside a TypeScript template literal (backtick string) in `html.ts`
- Regex `/\s+/` and `/^(\d+)(.)$/` lost their backslashes during template literal evaluation — `\s` → `s`, `\d` → `d`
- Browser received `/s+/` (splits on literal 's') and `/^(d+)(.)$/` (matches literal 'd') — completely wrong
- Token splitting never worked (no 's' characters in RLE data), so decoded rows were empty strings → nothing drawn
- Fix: double-escape all regex character classes — `\\s` and `\\d` in the TypeScript source
- This was the actual root cause across Tasks 28-30; the algorithm was correct but the escaping was not
- Files changed: `services/pixel-explorer/src/html.ts` (lines 626, 630)
- All 8 workspaces build clean
- Key: ANY regex with `\s`, `\d`, `\w`, `\b`, etc. inside a JS template literal needs `\\s`, `\\d`, etc.

## Task 31: Animation Playback Controls

- Added clip animation playback to pixel-explorer detail view
- Client-side animation engine: easing functions (step/linear/ease-in/out/in-out), keyframe interpolation, clip time computation (loop/once/pingpong)
- Controls: Play/Pause button, Loop toggle, clip selector dropdown (when multiple clips), progress bar (clickable for seeking), time display
- Animation applies all track properties: offsetX, offsetY, rotation, scale, opacity, frame
- Uses requestAnimationFrame for smooth 60fps playback
- Progress bar shows current position, clicking seeks to that point
- Switching clips resets playback; navigating away stops animation
- CSS: `.anim-controls` bar below preview canvas, styled to match dark theme
- All 8 workspaces build clean
- Files changed: `services/pixel-explorer/src/html.ts`

## Task 32: Move Generation Logic to @loreweave/pixel

- Moved generation logic from pixel-explorer's `apiGenerate` into `@loreweave/pixel` as `generator.ts`
- New exports: `generatePixelAsset()`, `GeneratePixelOptions`, `GeneratePixelResult`, `PixelAssetType`
- `generatePixelAsset(adapter, options)` accepts any `AgentAdapter` via dependency injection:
  - Design-time: CLI adapter writes files to disk
  - Runtime: future API adapter generates assets in memory (cached)
- Generator handles: system prompt construction, context file loading, palette injection, post-generation validation
- `GeneratePixelOptions` supports both file-based context loading (`assetDir`, `contextDir`, `palette`) and pre-loaded content (`context.guide`, `context.schema`, `context.paletteJson`) for runtime use
- Added `@loreweave/agents` as dependency of `@loreweave/pixel` (for `AgentAdapter` type)
- Updated pixel tsconfig.json to reference agents
- Slimmed pixel-explorer's `apiGenerate` from ~210 lines to ~60 lines — now a thin HTTP wrapper
- All 8 workspaces build clean
- Files created: `packages/pixel/src/generator.ts`
- Files changed: `packages/pixel/package.json`, `packages/pixel/tsconfig.json`, `packages/pixel/src/index.ts`, `services/pixel-explorer/src/server.ts`
- Key: pixel now depends on agents (agents has no deps, lightweight); build order already correct (agents before pixel)

## Task 33: Auto-Sizing System (Detail Level + Sprite Archetypes)

- Created `packages/pixel/src/sizing.ts` — the auto-sizing engine:
  - `DetailLevel` type: `'low' | 'standard' | 'high'` mapping to PPU 16, 32, 64
  - `SpriteArchetype` interface: key, label, worldWidth/worldHeight (in tiles), description, keywords
  - 12 registered archetypes: icon, small-item, item, character, creature, large-creature, boss, prop, tree, building, large-building, tile
  - `computeDimensions(archetype, detailLevel)` — returns pixel width/height/ppu from world-space dims × PPU
  - `inferArchetype(prompt)` — keyword matching to determine what object category a prompt describes
  - `buildSizingTable(detailLevel)` — generates a markdown reference table for LLM system prompts
- Updated `packages/pixel/src/generator.ts`:
  - Added `detailLevel` option to `GeneratePixelOptions` (defaults to 'standard')
  - Generator auto-infers archetype from prompt, computes dimensions, injects sizing table + specific dims into system prompt
  - `GeneratePixelResult` now includes `sizing?: ComputedDimensions` so callers see what was auto-computed
  - System prompt tells the LLM exactly what dimensions to use based on the archetype table
- Updated `services/pixel-explorer/src/server.ts`:
  - `apiGenerate` accepts `detailLevel` in request body, passes through to generator
  - Success response includes `sizing` object
- Updated `services/pixel-explorer/src/html.ts`:
  - Added Detail Level dropdown (Low/Standard/High) to Generate panel, replacing the need to specify dimensions in the prompt
  - Added live sizing hint below the dropdown — shows inferred archetype and computed dimensions as user types
  - Client-side `inferArchetypeClient()` mirrors server-side archetype matching for instant feedback
  - Updated prompt placeholder to not mention dimensions
- Exported new types/functions from `packages/pixel/src/index.ts`
- All 8 workspaces build clean
- Files created: `packages/pixel/src/sizing.ts`
- Files changed: `packages/pixel/src/generator.ts`, `packages/pixel/src/index.ts`, `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/html.ts`
- Key: user never specifies dimensions — just picks a detail level once and describes what they want
- Key: archetype keywords list is extensible; adding new object types is just adding to the array

## Task 34: Asset Manager Restructure

- Restructured pixel-explorer from flat file browser to managed asset system
- **New types** in `@loreweave/types`: `AssetMeta`, `AssetView`, `AssetReference`, `AssetType`, `AssetReferenceRole`
  - Each managed asset has `version`, `name`, `type`, `views` (Record of named views → pixel files), `references` (links to other assets), `tags`, `palette`, `detailLevel`
  - Reference roles: `component`, `variant`, `attachment`, `dependency`
- **New loader functions** in `@loreweave/pixel`:
  - `findManagedAssets(dir)` — walks directory tree for folders containing `asset.json`
  - `findUnmanagedPixelFiles(dir, managedFolders)` — finds pixel files NOT inside managed folders
  - `loadAssetMeta(folder)` / `writeAssetMeta(folder, meta)` — read/write `asset.json`
  - `createManagedAsset(assetDir, meta)` — creates new folder + `asset.json`, validates uniqueness
  - `slugifyAssetName(name)` — converts display name to folder-safe slug
- **Migration module** (`services/pixel-explorer/src/migration.ts`):
  - Runs at startup, groups unmanaged pixel files by name prefix
  - Creates managed asset folders with views derived from file names (e.g., `campfire_unlit` → view "unlit")
  - Moves pixel files into asset folders, skips palettes (they stay as shared resources)
  - Idempotent — won't touch already-managed assets
- **Updated REST API**:
  - `GET /api/assets` → returns `{ managed: [...], unmanaged: {...} }` instead of flat file list
  - `POST /api/assets` → create new managed asset (name, type, description)
  - `PUT /api/asset?path=<folder>` → update asset metadata (references, tags, etc.)
  - `GET /api/asset/file?path=<file>` → direct pixel file access (split from managed asset endpoint)
  - `GET /api/asset?path=<folder>` → returns full managed asset with all view data loaded
  - `POST /api/generate` → now accepts `assetFolder` and `viewName` to generate into managed assets, auto-updates `asset.json`
- **Updated generator**: added `outputDir` option to `GeneratePixelOptions` for generating into specific folders
- **Reworked web UI**:
  - Gallery shows managed asset cards with name, type badge, view count, ref count, description
  - "+" button opens modal to create new named assets
  - Asset detail page has view tabs, inline generation panel ("+ Add View"), reference list with navigation
  - Generation happens from within asset detail — generates a named view and updates the asset
  - Unmanaged files shown in separate "Unmanaged Files" section
  - Removed standalone Generate page — generation is now asset-scoped
  - File watcher now also detects `asset.json` changes
- All 8 workspaces build clean, lint passes (pre-existing warnings only), tests pass
- Files created: `services/pixel-explorer/src/migration.ts`
- Files changed: `packages/types/src/pixel.ts`, `packages/types/src/index.ts`, `packages/pixel/src/loader.ts`, `packages/pixel/src/index.ts`, `packages/pixel/src/generator.ts`, `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/html.ts`, `services/pixel-explorer/src/index.ts`
- Key: folder-per-asset structure mirrors how users think about assets (campfire = one thing with multiple views)
- Key: references enable composition — fire + smoke can be linked as components of a campfire asset

## Task 35: Full CRUD for Assets and Views

- Added `deleteManagedAsset()`, `deleteAssetView()`, `renameAssetView()` to `packages/pixel/src/loader.ts`
  - `deleteManagedAsset` verifies asset.json exists (safety check) before rm -rf
  - `deleteAssetView` removes pixel file + view entry from asset.json, resets defaultView if needed
  - `renameAssetView` changes view key in metadata, handles defaultView update
- New API endpoints in `services/pixel-explorer/src/server.ts`:
  - `DELETE /api/asset?path=<folder>` — delete entire managed asset
  - `DELETE /api/asset/view?path=<folder>&view=<name>` — delete single view
  - `PUT /api/asset/view?path=<folder>&view=<name>` — rename view (body: `{newName, newLabel?}`)
- Web UI additions in `services/pixel-explorer/src/html.ts`:
  - **Delete Asset** button (red) on asset detail header — confirms via modal dialog
  - **Delete View** (x) button on each view tab — confirms via modal dialog
  - **Edit Asset** (pencil icon) on asset detail header — opens modal to edit name, description, tags
  - `showConfirm()` reusable confirmation dialog with cancel/confirm
  - `showEditAssetModal()` modal for editing asset metadata
- All 8 workspaces build clean, lint passes (pre-existing warnings only), tests pass
- Files changed: `packages/pixel/src/loader.ts`, `packages/pixel/src/index.ts`, `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/html.ts`

## Task 36: Fix Duplicate Palettes + IndexedDB Preferences

- Root cause of duplicate palettes: `wireGenerateForm()` fetched `/api/palettes` asynchronously and appended `<option>` elements. When `loadAssetDetail` was called twice in quick succession (WebSocket file-change reload), both async callbacks resolved and appended to the same `<select>` — doubling the list
- Fix: `preloadPalettes()` now caches the palette list in `cachedPaletteList` array. `wireGenerateForm()` populates the select synchronously from this cache — no async race, no duplicates
- Added IndexedDB persistence for generate form preferences:
  - Uses `PixelExplorerPrefs` database, `prefs` object store, single `generate` key
  - `openPrefsDb()` creates DB + store on first use (version 1 upgrade)
  - `loadPrefs()` / `savePrefs()` read/write the preference object
  - Stored fields: `detailLevel`, `palette`, `model`
  - Preferences restored in `wireGenerateForm()` after populating selects
  - Preferences saved on `change` event for all three dropdowns
  - Survives page refresh, browser restart, and cross-session navigation
- All 8 workspaces build clean
- Files changed: `services/pixel-explorer/src/html.ts`

## Task 37: Custom Colors (64 + 8) + Parallel Generation

- **Custom Colors per Asset**:
  - Added `customColors?: Record<string, PixelColor>` to `AssetMeta` (max 8 entries)
  - Standard palette soft-capped at 64 colors (validator warns above 64, errors above 72)
  - Added `validateCustomColors()`: enforces 8 max, single-char keys, valid hex, warns on palette collisions
  - Generator injects custom colors into LLM system prompt
  - Server loads custom colors from asset.json before generation, passes to generator
  - Client-side renderer merges custom colors on top of palette entries for all rendering
  - Custom colors editor in asset detail: click-to-edit color picker, add/remove, auto-key suggestion
- **Parallel Generation**:
  - Server tracks active tasks in `Map<string, GenerationTask>` keyed by `folder/viewName`
  - `POST /api/generate` returns 202 Accepted immediately, runs generation in background
  - `GET /api/generate/status` endpoint for polling active tasks
  - Duplicate generation for same asset/view rejected with 409
  - `GET /api/assets` includes `generating: string[]` array
  - WebSocket broadcasts `generate-start` and `generate-complete` events
  - Gallery shows spinner overlay on cards for generating assets
  - `updateGallerySpinners()` does surgical DOM updates (no full re-render)
  - Generate form button stays disabled until WebSocket completion event
  - User can navigate away — spinner persists in gallery
- All 8 workspaces build clean
- Files changed: `packages/types/src/pixel.ts`, `packages/pixel/src/validator.ts`, `packages/pixel/src/index.ts`, `packages/pixel/src/generator.ts`, `services/pixel-explorer/src/server.ts`, `services/pixel-explorer/src/html.ts`
- Key: 202 pattern enables parallel generation — each request returns instantly, WebSocket notifies completion
- Key: Custom colors are asset-level (not view-level) — all views share the same custom palette extensions

## Task 38: Game Engine Foundation

- Built three core engine packages from empty stubs to working implementations
- **@loreweave/types — game.ts**: 60+ types covering full game engine surface
  - World: WorldSeed, ChunkCoord, TileCoord, WorldTile, Chunk, ChunkStage
  - Terrain: BiomeType (7), SurfaceType (9), ResourceType (9), FeatureType (23), StructureID (6)
  - D&D 5e: AbilityScores, Character, Item, InventorySlot, SkillName (18), Condition (14)
  - Actions: GameAction (10 types), ActionResult, GameEffect (9-variant discriminated union)
  - Time/weather: GameTime, TimeOfDay (8), Weather (7), Season (4)
  - Narrative: NarrativeContext, EntityRef, CharacterSummary, ExitInfo
  - Constants: CHUNK_SIZE=32, DIRECTION_OFFSETS, SKILL_ABILITIES
- **@loreweave/rules**: D&D 5e rules engine in 3 modules
  - `dice.ts` — xorshift32 PRNG, createRng, rollDie/rollDice, d20/advantage/disadvantage, parseDiceNotation
  - `abilities.ts` — abilityModifier, proficiencyBonus, skillModifier, abilityCheck, skillCheck, savingThrow, standardArray, rollAbilityScore
  - `combat.ts` — rollInitiative, initiativeOrder, meleeAttack, rangedAttack, applyDamage, applyHealing, isUnconscious, isDead, passivePerception
- **@loreweave/world**: World generation engine in 8 modules
  - `rng.ts` — FNV-1a hashing, positionalSeed, positionalRandom, seedFromString
  - `noise.ts` — OpenSimplex2 noise (2D), octaveNoise, ridgedNoise
  - `biomes.ts` — BIOME_TABLE (7 biomes), selectBiome (3D closest-match), computeElevation
  - `surface.ts` — determineSurface (biome+elevation rules), isWalkable
  - `resources.ts` — determineResources (biome resource tables, river resources)
  - `features.ts` — determineFeatures (5-step decoration system, biome feature tables)
  - `spawns.ts` — SPAWN_TABLE (7 entities), generateSpawnPoints
  - `chunk-generator.ts` — Full 7-stage pipeline (biomes→elevation→surface→resources→structures→features→spawns), noise caching, biome blending, river overlay
  - `world.ts` — World class: chunk cache, lazy generation, tile access, time/weather/season management, world events
- All 8 workspaces build clean, all tests pass, lint passes (pre-existing warnings only)
- Files created: `packages/types/src/game.ts`, `packages/rules/src/dice.ts`, `packages/rules/src/abilities.ts`, `packages/rules/src/combat.ts`, `packages/world/src/rng.ts`, `packages/world/src/noise.ts`, `packages/world/src/biomes.ts`, `packages/world/src/surface.ts`, `packages/world/src/resources.ts`, `packages/world/src/features.ts`, `packages/world/src/spawns.ts`, `packages/world/src/chunk-generator.ts`, `packages/world/src/world.ts`
- Files changed: `packages/types/src/index.ts`, `packages/rules/src/index.ts`, `packages/world/src/index.ts`
- Key: OpenSimplex2 for noise (no axis-aligned artifacts, public domain)
- Key: All functions are pure — deterministic output for same input (critical for multiplayer)
- Key: Structure placement is a no-op placeholder — needs multi-chunk coordination layer
- Key: World class provides lazy chunk generation with configurable load radius

## Task 39: DungeonMaster — Narrative Engine (Loremaster as DM)

- Built the full `@loreweave/narrative` package from empty stub to working DM system
- **New types** in `packages/types/src/game.ts`:
  - `DMConfig` — DM engine configuration (view radius, DC defaults, time costs)
  - `NarrativeAdapter` — interface for LLM text generation (`generate(system, user) → string`)
  - `DMResponse` — full turn result (narrative, action, effects, context, party state, time)
  - `GameSession` — active game state (world, party, quests, entities, combat state)
  - `WorldAccess` — interface abstracting World class methods (prevents types→world circular dep)
- **5 modules** in `packages/narrative/src/`:
  - `intent-parser.ts` — keyword-based player text → GameAction. 10 action patterns, direction keywords, entity/item resolution by substring match. Deterministic, no LLM — fast and free.
  - `context-assembler.ts` — builds NarrativeContext from GameSession. Scans exits, summarizes party, gathers quest hints for nearby objectives.
  - `action-resolver.ts` — routes GameActions through the rules engine. 10 action resolvers (move, look, attack, search, rest, talk, pickup, drop, use_item, craft). `applyEffects()` returns new character state — no mutation.
  - `prompt-builder.ts` — converts NarrativeContext + ActionResult into system/user prompt pair. System prompt: DM persona + rules. User prompt: location, environment, party, entities, exits, events, quests, action + mechanical results.
  - `dm.ts` — `DungeonMaster` class orchestrating the full turn loop: parse → resolve → apply effects → assemble context → build prompts → generate narrative. Also: `createSession()` factory, party/quest/entity management, `describeSurroundings()` for initial scene.
- Updated `packages/narrative/package.json`: added `@loreweave/rules` dependency
- Updated `packages/narrative/tsconfig.json`: added rules project reference
- Architecture follows spec exactly: Player Input → Intent Parser → Rules Engine → World State → LLM (narrative only)
- LLM never decides mechanical outcomes — dice rolls, combat, skill checks are all pre-resolved
- `NarrativeAdapter` interface is pluggable — any LLM backend (Anthropic API, local model, mock)
- All 8 workspaces build clean, all tests pass
- Files created: `packages/narrative/src/intent-parser.ts`, `packages/narrative/src/context-assembler.ts`, `packages/narrative/src/action-resolver.ts`, `packages/narrative/src/prompt-builder.ts`, `packages/narrative/src/dm.ts`
- Files changed: `packages/types/src/game.ts`, `packages/types/src/index.ts`, `packages/narrative/src/index.ts`, `packages/narrative/package.json`, `packages/narrative/tsconfig.json`
- Key: `WorldAccess` interface in types prevents circular dependency (types doesn't import world)
- Key: Intent parser is keyword-based (deterministic) — LLM is only used for narrative rendering
- Key: All action resolvers return effects arrays, not mutations — pure functions throughout
