---
compressed: true
---

# Scribe — Daily Log — 2026-03-26

## Task 1: Pixel Format v1 Spec Overview
Confirmed spec capabilities for stevenic: palettes (§6) map single-char symbols to colors, layers exist at scene level (§12) for compositing sprites.

## Task 2: Sprite Scaling & Depth Composition Analysis
Identified spec gaps: `scale` missing on `PixelSceneItem` (§12.5) and no `baseScale` on sprites. Proposed two additions plus depth layers, y-sort, camera/viewport, fog ideas.

## Task 3: Full Pixel Format v1 Revision Proposal
Proposed 18 features across 4 categories to make Pixel Format v1 LLM-native: depth/composition (6), LLM generation (8), promoted from v2 (4). Core insight: single-char grid model is the LLM optimization — everything builds on keeping it simple. Excluded: multi-char keys, binary encoding, skeletal animation.

## Task 4: Aseprite & Pyxel Edit Competitive Analysis
Validated our text-first design. Added 3 features from competitors: animation tags/clips (Aseprite), named regions/slices (Aseprite), tile transforms (Pyxel Edit). Skipped: layer grouping, onion skinning, Lua scripting, binary serialization. Total features: 21.

## Task 5: Definitive Sprite Format Design
Designed complete sprite format: symmetry hints (50% token reduction), vertical frame stacking, named regions, advisory constraints, generation metadata, variant rules, palette aliases/ramps, baseScale. Open questions: half vs full pixels, variant complexity, animation structure, constraint enforcement.

## Task 6: Rendering Pipeline Design
Decided: no .png on disk — .pixel.json is source-of-truth, rasterize to canvas at load time. Canvas 2D blend modes handle lighting/effects for v1. WebGL deferred. Scale chain: `canvas.scale × layer.scrollFactor × item.scale × sprite.baseScale`.

## Task 7: Complete Pixel Format v1 Spec Rewrite
Full rewrite of `pixel_v_1_spec.md` (~900 lines). Resolved open questions: full pixels with advisory symmetry hints, simple variants (palette+scale), vertical frame stacking, advisory constraints. 6 file types, 17 sections, keyframe animation, attachment points, emitter configs.
- **Files changed:** `pixel_v_1_spec.md`

## Task 8: LLM Generation Guide
Created `pixel_format_llm_guide.md` — compact (~200 lines) example-driven reference for LLMs generating .pixel assets. Examples over prose, common generation task recipes.
- **Files created:** `pixel_format_llm_guide.md`

## Task 9: Pixel Format v1 JSON Schema
Created `pixel-schema.json` — JSON Schema (draft 2020-12) with 35 `$defs`, top-level `oneOf` by format field, strict validation, descriptions on every definition.
- **Files created:** `pixel-schema.json`

## Task 10: LLMD Schema Compression
Ran `schema2llmd` to produce `pixel-schema.llmd` (~629 tokens, 127 lines). Note: tool needs `$defs` support (one-line fix).
- **Files created:** `pixel-schema.llmd`

## Task 11: Pixel Asset Sizing Guide
Recommended dimensions: xl (64×64) structures, lg (32×48) characters, md (32×32) small structures, sm (16×16) items/tiles, xs (8×8) icons. Proposed size class vocabulary for generation prompts.

## Task 12: PPU (Pixels Per Unit) Proposal
Proposed `ppu` field — "DPI for a tile-based world." Anchors scale chain to world space. Default 32. Formula: `world size = pixel dimensions ÷ ppu`. Scene-level `referencePpu` for auto-scaling.

## Task 12b: Wisdom Distillation
Added 6 new WISDOM entries: propose before editing specs, text is the feature, competitive analysis skips matter, multi-tier docs, .pixel.json is source-of-truth, advisory constraints.
- **Files changed:** `.teammates/scribe/WISDOM.md`

## Task 13: PPU Spec Amendment
Added `ppu` to PixelSprite (§5.2), PixelTileset (§7.1), `referencePpu` to scene canvas (§9.2). New §5.12 PPU section. Updated scale chain formula, validation rules, TypeScript types, LLM guide.
- **Files changed:** `pixel_v_1_spec.md`, `pixel_format_llm_guide.md`
- **Handoff:** @beacon for schema/types/LLMD updates

## Task 14: Emitter PPU Amendment
Added `ppu` to emitter fields (§10.3), new §10.4 Emitter PPU, new §11.6 emitter spatial scaling rendering rule, validation rule, TypeScript type.
- **Files changed:** `pixel_v_1_spec.md`
- **Handoff:** @beacon for schema/types/LLMD updates

## Task 15: Wisdom Distillation (end-of-day)
Added 2 entries: "Anchor abstract scale chains to world space" (PPU lesson), "Start focused, expand on signal" (2→18→21→rewrite pattern). Total: 17 entries.
- **Files changed:** `.teammates/scribe/WISDOM.md`

## Task 16: Minecraft Biome & Block Count Research
Minecraft: ~64 biomes, ~900+ block types. LoreWeave: 7 biomes, tile types TBD.

## Task 17: Lit/Unlit States — Views vs. Separate Assets
Recommended: views of the same asset, not separate assets. Same identity in different states = views; independent identity = separate asset.

## Task 18: Asset Design Guide
Created `asset_design_guide.md` with 7 rules: views vs assets, when to reference, naming views, asset granularity, choosing asset types, palette sharing, detail consistency.
- **Files created:** `asset_design_guide.md`

## Task 19: 64 + 8 Custom Colors Palette Analysis
Proposed: cap base palettes at 64 keys, reserve 8 for per-asset custom colors. Advantages: shared vocabulary + controlled variation, power-of-two base. Fantasy72 could be deprecated. Pending approval.

## Task 20: Settlement Generation Design Proposal
Designed 4-tier hierarchy: Hamlet (20×20), Village (40×40), Town (80×80), City (128×128). Settlements as zones with 5-stage pipeline (roads → lots → buildings → infrastructure → NPCs). Two-tier map model (overworld + interior scenes). 14 building types. Rejected inline interiors for v1.

## Task 21: Text-First World Structure Analysis
Proposed two-layer text model: (1) Tile Descriptors — deterministic lookup tables for semantic tags, (2) Places — text-adventure atom with exits, entities, features. Three source types: procedural, generated-then-cached, authored. Key insight: descriptions as write-once assets.

## Task 22: Text-First World Architecture — LLM-Live Generation
Refined based on stevenic's decisions: always LLM-live (no cached descriptions), persistent+mutable world, budget-constrained context. Designed LocationRecord + event-sourced state mutations + recovery rules. ~730 token input budget estimate.

## Task 23: Refined World Architecture — Algorithmic + LLM Budgets
stevenic's refinements: text 16-32k tokens, pixel 64k tokens, algorithmic location generation (archetype libraries, zero LLM cost). Three-tier cost model: algorithmic (free) → text LLM (16-32k) → pixel LLM (64k, first visit only). Template-based scene assembly.

## Task 24: Architecture Decisions — Spec Amendment
Formalized randomness algorithms in `world_generation_spec.md`: FNV-1a hashing (§2.1-2.2), xorshift32 PRNG (§2.2), OpenSimplex2 noise (§2.3), algorithm stack summary (§2.4), expanded determinism guarantees (§12).
- **Files changed:** `world_generation_spec.md`

## Task 25: Writer Teammate Proposal — "Bard"
Proposed Bard (Narrative Designer) to fill content gap: nobody owns lore, stories, factions, quest design, NPC profiles. Boundary: Bard writes *what*, Lexicon designs *how*, Loremaster ensures *mechanics*, Scribe specs *format*.

## Task 26: Created Bard Teammate
Created `.teammates/bard/` with SOUL.md, WISDOM.md, onboarding memory. Updated README.md and CROSS-TEAM.md with roster, dependency flow, ownership.
- **Files created:** `.teammates/bard/SOUL.md`, `.teammates/bard/WISDOM.md`, `.teammates/bard/memory/2026-03-26.md`
- **Files updated:** `.teammates/README.md`, `.teammates/CROSS-TEAM.md`

## Task 27: Narrative Engine Spec Amendment
Amended `loreweave_spec.md` §4.3 (intent parser — 10 action types, keyword tables), §4.4 (narrative engine — 6 sub-sections covering DM loop, action resolution, context assembly, prompt architecture, NarrativeAdapter, config defaults), §9 (game loop — session init, 10-step turn processing, session management). Verified against implementation.
- **Files changed:** `loreweave_spec.md`

## Task 28: Update Bard Ownership Paths
Updated CROSS-TEAM.md and README.md: `world/` → `worlds/*/` paths for Bard after multi-world restructure.
- **Files changed:** `.teammates/CROSS-TEAM.md`, `.teammates/README.md`

## Task 29: Created worlds/shared/ — Standard Archetype Library
Created `worlds/shared/` with archetype format schema, 12 location archetypes (taverns, shops, temples, residences, civic, wilderness), 9 NPC archetypes (merchants, authority, craft, service, wanderers), 3 vocabulary tables (biome descriptors, time of day, building styles). Probability tiers: required/common/rare.
- **Files created:** ~21 files in `worlds/shared/`
- **Files updated:** `.teammates/CROSS-TEAM.md`
