# Scribe — Daily Log — 2026-03-26

## Task 1: Pixel Format v1 Spec Overview

- **What:** Provided stevenic with an overview of the Pixel Format v1 spec, specifically addressing palettes and layer composition capabilities.
- **Context:** stevenic is exploring building a .pixel resource explorer and wanted to confirm the spec's capabilities before proceeding.
- **Key points communicated:** The spec has both palettes (§6) and layers (§12, scene files). Palettes map single-char symbols to colors. Layers exist at the scene level for compositing multiple sprites — not within individual sprites.
- **Files changed:** None (read-only task)

## Task 2: Sprite Scaling & Depth Composition Analysis

- **What:** Analyzed stevenic's request for per-sprite scaling (zoom in/out) with embedded base scale indicators for depth composition. Provided a detailed spec amendment proposal.
- **Context:** stevenic wants to compose scenes with apparent depth by scaling sprites up (foreground) and down (background). Asked how hard it would be and requested additional ideas.
- **Key analysis:**
  - Algorithmic nearest-neighbor scaling is trivial — multiply each pixel by scale factor
  - Current spec gap: `scale` exists on `canvas` (§12.3) but NOT on `PixelSceneItem` (§12.5)
  - Current spec gap: no `baseScale` or reference scale on `PixelSprite` (§15)
  - Proposed two spec additions: (1) `scale` field on scene items, (2) `baseScale` field on sprites
  - Also proposed ideas: depth layers convention, auto-depth from y-position, camera/viewport concept, fog/desaturation for distance
- **Files changed:** None (analysis/proposal only — pending stevenic approval before spec edit)

## Task 3: Full Pixel Format v1 Revision Proposal — LLM-Native Format

- **What:** Delivered comprehensive proposal to transform Pixel Format v1 into the first pixel art format designed for LLM generation. 18 features across 4 categories.
- **Context:** stevenic said "go for everything out the gate" on depth features AND wants to discuss v2 ideas. Vision: "a killer new pixel art format optimized for generation by LLMs."
- **Proposal details:**
  - **Depth & Composition (6):** `scale` on scene items, `baseScale` on sprites, parallax `scrollFactor` on layers, y-sort depth, camera/viewport on canvas, `groundY` shadow anchor
  - **LLM Generation (8):** semantic palette aliases, color ramps, symmetry hints (horizontal/vertical/both with half-data generation), sprite templates with named regions, generation constraints (`maxColors`, `outline`, `noIslands`), variant rules (palette swaps, scale, row removal), tile connectivity (Wang tile edge matching), generation metadata (prompt, model, seed, lineage)
  - **Promoted from v2 (4):** animation strips (frames stacked vertically in pixels array), tilemaps (`pixel-tilemap-v1` new file type with char-grid tile placement), blend modes on layers (normal/multiply/screen/overlay), layer opacity
  - **Deliberately excluded:** multi-char palette keys (breaks char-grid model), binary encoding (text IS the feature), skeletal animation, shaders, SVG, sprite-internal layers
- **Key design insight:** The single-char grid model is the core LLM optimization. Everything builds on keeping that simple. Symmetry hints cut token count in half. Templates give LLMs structural guidance. Ramps give LLMs a vocabulary for shading.
- **Status:** Awaiting stevenic approval before editing spec
- **Files changed:** None (proposal only)

## Task 4: Aseprite & Pyxel Edit Competitive Analysis

- **What:** Analyzed stevenic's deep review of Aseprite and Pyxel Edit formats against our Pixel Format v1 and the pending 18-feature proposal. Identified what to steal, what to skip, and what validates our approach.
- **Context:** stevenic shared a detailed systems-level review of both formats through an LLM-generation lens. Asked what we can take away or add.
- **Key takeaways:**
  - Analysis validates our core design decisions: text-first, declarative JSON, git-diffable, symbolic references
  - 3 new features worth adding from competitors:
    1. **Animation tags/clips** (from Aseprite) — named sub-sequences within animation strips with playback modes (forward/reverse/pingpong)
    2. **Named regions/slices** (from Aseprite) — named rectangular regions for attachment points, multiple hitboxes, LLM-driven composition
    3. **Tile transforms** (from Pyxel Edit) — flip/rotate on tilemap placement to avoid duplicating tile variants
  - Features deliberately NOT taken: layer grouping, onion skinning, Lua scripting, binary serialization, skeletal composition
  - Updated total feature count: 21 (was 18)
  - Strategic insight: Aseprite/Pyxel are artist tools with file formats; we're building a file format for artists AND LLMs. Our moat is: text IS the art, declarative not procedural, symbolic not literal, diffable by design, constrained generation.
- **Status:** Awaiting stevenic approval on full 21-feature set before editing spec
- **Files changed:** None (analysis only)

## Task 5: Definitive Sprite Format Design — LLM-Optimized

- **What:** Designed the complete, definitive sprite format for Pixel Format v1, answering stevenic's question: "what does an individual sprite look like that an LLM can generate and transform?"
- **Context:** stevenic wants to nail down the exact format. Agrees on avoiding binary, wants RLE support. This is the atomic unit — everything else builds on it.
- **Key design decisions:**
  - **Symmetry hints** (`horizontal`/`vertical`/`both`/`none`) — LLM generates only half the pixel grid, renderer mirrors. 50% token reduction for symmetrical assets.
  - **Animation via vertical stacking** — frames stacked in `pixels` array, `height` = frame height, total rows = height × frameCount. Clips with named tags and playback modes (loop/once/pingpong).
  - **Regions** (named rectangles) — enables partial regeneration ("just redo the head"), attachment points, multiple hitboxes.
  - **Constraints** — `maxColors`, `outline`, `noIslands` — generation quality hints for LLMs.
  - **Generation metadata** — prompt, model, seed, parent lineage for variant tracking.
  - **Variant rules** (declarative) — palette swaps and scale overrides without duplicating pixel data.
  - **Palette enhancements** — semantic aliases (char→name mapping) and color ramps (ordered gradients) in palette files.
  - **baseScale** on sprites for scene depth composition.
  - All new fields are optional — format is fully backwards compatible with existing sprites.
  - Deliberately excluded from sprites: internal layers, multi-char keys, binary encoding, skeletal animation, blend modes, procedural rules.
- **Open decision points posed to stevenic:**
  1. Symmetry: store half pixels or full pixels with hint?
  2. Variants: simple (palette+scale) or complex (row removal, region replacement)?
  3. Animation: vertical stacking in one array or structured array-of-arrays?
  4. Constraints: hard reject or advisory warn?
- **Files changed:** None (design proposal only — awaiting stevenic decisions before spec edit)

## Task 6: Rendering Pipeline Design — .pixel vs .png

- **What:** Answered stevenic's question about whether sprites should have both `.pixel.json` source and `.png` pre-rendered files, scaling capabilities, and lighting/overlay composition. Designed the two-tier rendering model.
- **Context:** stevenic wants browser-based JS rendering. Asked about performance, rescaling, and overlaying PNGs with lighting effects.
- **Key design decisions:**
  - **No `.png` on disk** — `.pixel.json` is source-of-truth. Rasterize to offscreen `<canvas>` at load time, cache as `ImageBitmap`. No drift risk, no build step friction.
  - **Scaling** — `ctx.drawImage()` with `imageSmoothingEnabled = false` (nearest-neighbor). Scale chain: `canvas.scale × layer.scrollFactor × item.scale × sprite.baseScale`.
  - **Lighting/effects** — Canvas 2D blend modes (`multiply`, `lighter`, `screen`, `overlay`) handle tinting, additive lighting, glow, shadows, and masking natively. No WebGL needed for v1.
  - **Lighting assets are just sprites** — a warm light overlay is a sprite with semi-transparent palette colors, composed via blend modes at the scene layer level.
  - **WebGL deferred** — only needed for per-pixel normal map lighting, shader-based palette swaps, particles, or post-processing. Canvas 2D covers v1 needs.
  - **`.png` export** — only as a derived build artifact (thumbnails, marketplace, docs), never as source.
- **Spec implications:** `blend` mode on scene layers (already in 21-feature proposal) is the key addition. No new file types needed.
- **Files changed:** None (design discussion only)
- **Still open:** 4 sprite format decisions from Task 5, plus whether to write a "Rendering Pipeline" spec section

## Task 7: Complete Pixel Format v1 Spec Rewrite

- **What:** Rewrote `pixel_v_1_spec.md` from scratch — comprehensive v1 spec incorporating all 21+ discussed features plus keyframe animation, attachment points, and particle emitters.
- **Context:** stevenic approved "go for everything out the gate" and asked to rewrite the spec. Also asked about keyframe animation, skeletons, and particle physics.
- **Key design decisions made (resolving open questions):**
  - **Symmetry:** Full pixels stored, hint is advisory for generators (simpler, robust)
  - **Variants:** Simple (palette swap + scale only)
  - **Animation:** Vertical frame stacking with `frameCount` (compact, LLM-friendly)
  - **Constraints:** Advisory (generators should respect, validators warn, never reject)
  - **Keyframes:** Continuous interpolation for position/rotation/scale/opacity; step interpolation for frame tracks (no pixel blending)
  - **Skeletons → Attachment points:** Lightweight named positions, not full bone/IK; per-frame overrides supported
  - **Particles → Emitter configs:** Standalone files or inline on scene items; runtime handles physics
- **Spec structure (17 sections):**
  - 6 file types: sprite, tileset, tilemap, scene, palette, emitter
  - Sprites: layers, frames, clips, symmetry, regions, attachments, constraints, generation meta, variants, baseScale
  - Palettes: aliases, ramps
  - Tilesets: connectivity (Wang tiles)
  - Tilemaps: legend grid, tile transforms (flip/rotate)
  - Scenes: camera/viewport, parallax, blend modes, y-sort, attachments, groundY shadows, emitters
  - Clips: keyframe tracks with easing functions, playback modes
  - Full TypeScript reference types
  - Rendering rules including browser pipeline
- **Files changed:** `pixel_v_1_spec.md` (complete rewrite, ~900 lines)

## Task 8: LLM Generation Guide — Compact Prompt Reference

- **What:** Created `pixel_format_llm_guide.md` — a compact, example-driven reference document designed to be included in an LLM's context when generating .pixel format assets.
- **Context:** stevenic asked for a much more compact .md file to show the LLM. "The model doesn't need a ton of words as much as a few examples."
- **Design principles:**
  - Examples over prose — every concept shown with a concrete JSON snippet
  - Covers the full format surface: sprites, palettes, RLE, layers, animation, tilesets, tilemaps, scenes
  - Ends with a "Common generation tasks" section — step-by-step recipes for: create sprite, palette swap, animate, compose depth scene, add lighting, use symmetry, build tilemap, attach equipment
  - Deliberately omits: rendering pipeline details, validation rules, TypeScript types, emitter physics, browser implementation — none of that helps an LLM generate assets
  - ~200 lines vs ~900 in the full spec
- **Files created:** `pixel_format_llm_guide.md`

## Task 9: Pixel Format v1 JSON Schema

- **What:** Created `pixel-schema.json` — a complete JSON Schema (draft 2020-12) formally defining all 6 Pixel Format v1 file types and their constituent structures.
- **Context:** stevenic referenced Adaptive Cards' compressed schema approach and asked for a `pixel-schema.json` that could be used to automatically generate LLM-facing documentation. The schema is the machine-readable counterpart to the prose spec.
- **Key details:**
  - 35 `$defs` covering every type: PixelSprite, PixelTileset, PixelTilemap, PixelScene, PixelPalette, PixelEmitter, plus all sub-types (SpriteLayer, Clip, Track, Keyframe, Region, Attachment, Variant, EmitterConfig, SceneItem, SceneLayer, Camera, TileRef, etc.)
  - Top-level `oneOf` discriminates by `format` field — a single schema validates any .pixel file
  - Validation constraints encoded: palette key patterns, color patterns, enums for encoding/easing/blend/symmetry/sort, min/max on numeric fields, required vs optional fields
  - Descriptions on every definition — these are the payload for auto-generated docs
  - `additionalProperties: false` on all types for strict validation
  - Derived from both the spec (`pixel_v_1_spec.md`) and the TypeScript types (`packages/types/src/pixel.ts`) — cross-checked for consistency
- **Files created:** `pixel-schema.json`

## Task 10: LLMD Schema Compression

- **What:** Ran the `schema2llmd` tool from `../llmd/tools/js/` against `pixel-schema.json` to produce a compressed `pixel-schema.llmd` file (~629 tokens).
- **Context:** stevenic asked to use the llmd schema compressor to create an auto-generated LLM-facing reference from the JSON Schema, similar to how Adaptive Cards compresses its schema.
- **Key details:**
  - `schema2llmd.js` expects `definitions` key but our schema uses `$defs` (JSON Schema draft 2020-12). Created a temporary compat copy to bridge the gap, then cleaned it up.
  - Output: 127 lines, ~629 tokens — covers all 6 file types, all properties with types and descriptions
  - Two sections: `@Objects.Properties` (lists required/optional props per type) and `@Properties` (describes each property once with type, description, defaults, allowed values)
  - This is significantly more compact than the ~200 line LLM guide or ~900 line full spec
- **Note for upstream:** The `schema2llmd.js` tool should be updated to also check for `$defs` (line 473: `definitions = schema.definitions || schema['$defs'] || {}`). This is a one-line fix for JSON Schema draft 2020-12 compatibility.
- **Files created:** `pixel-schema.llmd`

## Task 11: Pixel Asset Sizing Guide — Full Scale at 72 Colors

- **What:** Provided stevenic with recommended pixel dimensions for different asset classes when rendering at full scale with the fantasy72 palette.
- **Context:** stevenic asked what the right scale is for a lodge front view vs. a person vs. a potion bottle at full color. Needed concrete dimension guidance for asset generation.
- **Recommendations:**
  - **xl (64×64):** Large structures — lodge, castle wall, large tree. 72 colors gives 5-6 stop ramps per material.
  - **lg (32×48):** Characters (tall sprites). Classic RPG proportions scaled up for detail.
  - **md (32×32):** Small structures, equipment, medium objects.
  - **sm (16×16):** Items (potion, key), tiles. Enough for label + fill gradient + highlight at 72 colors.
  - **xs (8×8):** Icons, tiny objects. Silhouette + 2-stop shading.
- **Scene composition:** 256×192 canvas at scale 3 = 768×576 viewport. Lodge at 64×64 is ~2× character width — proportions read correctly.
- **LLM generation insight:** Proposed size class vocabulary (xs/sm/md/lg/xl) so generation prompts say "generate an xl lodge" instead of guessing dimensions.
- **Pending:** stevenic's decision on whether to formalize sizing guide in the spec.
- **Files changed:** None (design guidance only)

## Task 12: PPU (Pixels Per Unit) Proposal

- **What:** Analyzed stevenic's follow-up question about DPI/resolution for the pixel format. Proposed `ppu` (Pixels Per Unit) as a sprite-level field — the "DPI for a tile-based world."
- **Context:** stevenic pointed out that even with size classes, the format has no way to express what "1x" means. Scale chain exists but has no anchor to world space. An LLM can't reason about relative proportions without it.
- **Key proposal:**
  - `ppu` = Pixels Per Unit, where unit = 1 world tile
  - Formula: `world size (tiles) = pixel dimensions ÷ ppu`
  - Default: 32 (characters/items). Buildings at 16, icons at 64, terrain at 8.
  - Sprite-level field (self-describing, LLM-friendly)
  - Optional scene-level `referencePpu` for auto-scaling
  - Avoids "DPI" terminology since we're not dealing with print/inches
- **Spec changes needed:** Add `ppu` to PixelSprite and PixelTileset, optionally `referencePpu` to scene canvas. Update schema, LLM guide, and TypeScript types.
- **Status:** Awaiting stevenic approval before spec edit
- **Files changed:** None (proposal only)

## Task 12: Wisdom Distillation

- **What:** Updated WISDOM.md with 6 new entries distilled from 3/26 daily log. All 9 existing entries retained — still valid.
- **New entries:**
  1. **Propose before editing specs** — always present changes as proposal → approval → edit
  2. **Text is the feature for LLM-native formats** — single-char grid is the core constraint; reject anything that breaks it
  3. **Competitive analysis: skip matters as much as steal** — what NOT to take from Aseprite/Pyxel Edit was equally valuable
  4. **Multi-tier documentation from single source of truth** — spec → guide → LLMD schema, keep tiers in sync
  5. **Source-of-truth is .pixel.json, never .png** — render at load time, export only as derived artifact
  6. **Advisory constraints, not hard rejections** — generators should respect, validators warn, never reject
- **Files changed:** `.teammates/scribe/WISDOM.md`

## Task 13: PPU Spec Amendment

- **What:** Amended `pixel_v_1_spec.md` and `pixel_format_llm_guide.md` with the approved `ppu` (Pixels Per Unit) field. Handed off to @beacon for downstream updates.
- **Context:** stevenic approved the PPU proposal from Task 12 and asked to update the spec, then have Beacon update schema and types.
- **Spec changes:**
  - Added `ppu` (integer, default 32) to PixelSprite optional fields (§5.2)
  - New §5.12 "Pixels Per Unit (PPU)" — formula, reference table, scene-level `referencePpu` auto-scaling, LLM generation guidance
  - Bumped subsequent section numbers (5.13 Base scale, 5.14 Generation metadata)
  - Added `ppu` to PixelTileset optional fields (§7.1)
  - Added `referencePpu` to scene canvas (§9.2)
  - Updated scale chain formula in §9.4 and §11.5 to include `ppuFactor = sprite.ppu / canvas.referencePpu`
  - Added validation rules for `ppu` (sprite, tileset) and `referencePpu` (scene)
  - Updated TypeScript reference types: `ppu` on PixelSprite, `ppu` on PixelTileset, `referencePpu` on scene canvas
  - Updated LLM guide: `ppu` in metadata table, example JSON, and "Create a sprite" generation task
- **Files changed:** `pixel_v_1_spec.md`, `pixel_format_llm_guide.md`
- **Handoff:** @beacon — update `pixel-schema.json`, `packages/types/src/pixel.ts`, and re-run `schema2llmd` to regenerate `pixel-schema.llmd`

## Task 14: Emitter PPU Amendment

- **What:** Amended `pixel_v_1_spec.md` to add `ppu` field to emitters. Handed off to @beacon for downstream updates.
- **Context:** stevenic approved adding `ppu` to emitters — the only file type with freestanding spatial values (velocity, gravity) that didn't already inherit PPU from context.
- **Spec changes:**
  - Added `ppu` (integer > 0, default 32) to emitter fields table (§10.3) with spatial unit annotations on velocity/gravity descriptions
  - New §10.4 "Emitter PPU" — explains formula, ppuFactor scaling, inline emitter behavior
  - Added `ppu` to standalone emitter example (§10.1)
  - New §11.6 "Emitter spatial scaling" rendering rule for ppuFactor on particle physics
  - Bumped §11.6 Blend mode compositing to §11.7
  - Added `ppu` validation rule to emitter validation (§12)
  - Added `ppu?: number` to EmitterConfig TypeScript type (§13)
- **Files changed:** `pixel_v_1_spec.md`
- **Handoff:** @beacon — update `pixel-schema.json`, `packages/types/src/pixel.ts`, and re-run `schema2llmd` to regenerate `pixel-schema.llmd`

## Task 15: Wisdom Distillation (end-of-day)

- **What:** Reviewed WISDOM.md against full day's log (14 tasks) and 3/25 log (9 tasks). Confirmed all 15 existing entries still valid. Added 2 new entries.
- **New entries:**
  1. **Anchor abstract scale chains to world space** — PPU solved the missing world-space anchor for relative scaling. Any format with layered transforms needs an absolute reference.
  2. **Start focused, expand on signal** — The 2→18→21→rewrite escalation worked because stevenic explicitly signaled at each step. Don't self-limit, but don't over-propose.
- **Total entries:** 17
- **Files changed:** `.teammates/scribe/WISDOM.md`

## Task 16: Minecraft Biome & Block Count Research

- **What:** Answered stevenic's question about how many biomes and block types Minecraft has. Compared against LoreWeave's world generation spec.
- **Context:** stevenic asked while Beacon is working on restructuring the pixel explorer into a static asset manager. Likely thinking about content variety and scope for LoreWeave's world generation.
- **Key numbers:**
  - Minecraft: ~64 biomes (across Overworld, Nether, End), ~900+ distinct block types
  - LoreWeave (current spec): 7 biomes, block/tile types TBD
- **Files changed:** None (research/answer only)

## Task 17: Lit/Unlit States — Views vs. Separate Assets

- **What:** Answered stevenic's question about whether lit/unlit states of a campfire or torch should be views of the same asset or separate assets.
- **Context:** Beacon just restructured the pixel explorer into a managed asset system with folders, asset.json metadata, and multi-view support. stevenic wants to understand the right modeling pattern.
- **Recommendation:** Views of the same asset, not separate assets. Lit/unlit are visual states of the same world object — they share identity, palette, proportions, and scene placement. Separate assets are for things with independent identity (fire effect, smoke particles) that can be referenced/composed.
- **Design principle:** If it's the same thing in different states → views. If it's a different thing that attaches to something → separate asset + reference.
- **Files changed:** None (design guidance only)

## Task 18: Asset Design Guide

- **What:** Created `asset_design_guide.md` — a rules-of-thumb reference for asset organization decisions.
- **Context:** stevenic liked the views-vs-assets rule of thumb and asked to start a design guide collecting these patterns.
- **Content (7 rules):**
  1. **Views vs. Separate Assets** — same identity in different states = views; independent identity = separate asset
  2. **When to Use References** — reference when reusable and independent; don't reference single-use sub-components
  3. **Naming Views** — name by what changes (lit/unlit, open/closed), not redundantly (campfire_lit)
  4. **Asset Granularity** — an asset = smallest unit with independent world identity
  5. **Choosing Asset Types** — sprite (single object), tileset (repeating), tilemap (grid composition), scene (multi-asset composition)
  6. **Palette Sharing** — co-located assets should share palettes for visual coherence
  7. **Detail Level Consistency** — assets in the same scene should use the same detail level
- **Files created:** `asset_design_guide.md`

## Task 19: 64 + 8 Custom Colors Palette Analysis

- **What:** Analyzed whether to cap base palettes at 64 colors and reserve 8 single-char keys for per-asset custom colors.
- **Context:** stevenic asked about the advantage of stopping at 64 base colors instead of 72, given the spec's 72-key ceiling.
- **Key analysis:**
  - Spec allows exactly 72 single-char palette keys (0-9, A-Z, a-z, 10 symbols)
  - Current palettes: fantasy16, fantasy32, fantasy64, fantasy72
  - Recommended split: base palette uses 64 keys (0-9, A-Z, a-z, !, @), custom uses 8 keys (# $ % & * + = ?)
  - Advantages: shared visual vocabulary + controlled variation, cleaner LLM instructions (two-tier model), power-of-two base size
  - Trade-offs: 64 is already a lot for most assets, inline palette merging adds a code path
  - Fantasy72 could be deprecated — its extra 8 colors become per-asset custom entries
- **Spec changes proposed (pending approval):**
  - §4.2 — formalize base/custom key split
  - §5.12 — add `maxCustomColors` advisory constraint
  - LLM guide — update with two-tier generation instructions
- **Status:** Awaiting stevenic approval before spec amendment
- **Files changed:** None (analysis only)

## Task 20: Settlement Generation Design Proposal

- **What:** Designed comprehensive settlement generation system for villages, towns, and cities with walkable streets and visitable interiors.
- **Context:** stevenic asked how generation of villages, towns, and cities would work — wants streets to walk down, shops and pubs to visit.
- **Key design decisions:**
  - **4-tier hierarchy:** Hamlet (20×20), Village (40×40), Town (80×80), City (128×128)
  - **Settlements as zones, not structures:** Current §8.3 `village` is 3×5 tiles — far too small. Settlements claim multi-chunk regions with their own generation pipeline.
  - **5-stage settlement pipeline:** road network → lot subdivision → building placement → infrastructure (walls/gates/lampposts) → NPC population
  - **Road generation:** Constrained random walk from center point. Organic for villages, grid-ish for towns, district-based arterials for cities.
  - **Two-tier map model (recommended):** Overworld shows settlement from above; entering a building transitions to a separate interior scene (pixel-scene-v1). Classic RPG pattern. Interior detail independent of overworld scale.
  - **Building catalog:** 14 building types (house, inn, store, blacksmith, temple, guild hall, barracks, castle, etc.) with footprint sizes, minimum tier, and interior flags.
  - **NPC population tables:** Each building type spawns role-appropriate NPCs with daily schedules (home → work → tavern).
  - **Rejected alternative:** Inline interiors (Ultima VII style) — too complex for v1, requires rooftop transparency, z-ordering, scale issues.
- **Open questions posed:**
  1. Overworld perspective — top-down rooftops or front-facing facades?
  2. Interior scope for v1 — all buildings or only key ones?
  3. Settlement persistence model
  4. Biome-driven trade/economy for shops
  5. Approval to write full spec section
- **Spec impact:** New §8A in `world_generation_spec.md`, plus new "map transition" concept in core spec
- **Files changed:** None (proposal only — awaiting stevenic approval)

## Task 21: Text-First World Structure Analysis

- **What:** Analyzed how to structure the world using a mix of generative and static text assets, independent of the visual layer.
- **Context:** stevenic reframed the discussion — LoreWeave is a text adventure first, pixel art is optional. The existing specs have no text content model. WorldTile has data (biome, features) but zero text properties. The narrative engine takes structured state but there's no definition of what text the world contains.
- **Key design proposal — Two-Layer Text Model:**
  - **Layer 1: Tile Descriptors** — deterministic lookup tables mapping biome × features × time × weather → semantic tags (terrain, atmosphere, sounds, smells). The "text palette" — not prose, but vocabulary for the narrative engine.
  - **Layer 2: Places** — the text-adventure atom. Aggregates tiles into named, describable locations with exits, entities, and features. Three source types:
    - *Procedural/ephemeral* — wilderness tiles described on the fly, not persisted
    - *Generated-then-cached* — structures get LLM-generated descriptions on first visit, cached permanently (like pixel sprites: generate once, render many)
    - *Static/authored* — scenario content with hand-written polished descriptions
  - **PlaceDescription** model: short/long text + time/weather/state variants + generation hints
  - **Exit graph** — directional connections between places with descriptions, conditions, hidden flags
  - **Text asset files** — `.place.json` (location), `.zone.json` (connected place graph), `.vocabulary.json` (biome descriptor tables)
- **Key insight:** Descriptions are write-once assets, not live generation. LLM generates once, caches permanently. Narrative engine at play time does composition/adaptation, not full generation.
- **Open questions posed:**
  1. Wilderness place granularity (tile vs. cluster vs. region?)
  2. Interior scope for v1 (all buildings or key ones?)
  3. Description versioning (regenerate on prompt improvement?)
  4. Culture tables depth for v1
  5. Approval to write formal spec section §4.7 Text Content Model
- **Files changed:** None (analysis/proposal only — awaiting stevenic decisions)

## Task 22: Text-First World Architecture — LLM-Live Generation Model

- **What:** Refined the text-first world model based on stevenic's three design decisions. Designed the complete LLM-live generation architecture.
- **Context:** stevenic answered 3 key questions from Task 21:
  1. Always through the LLM — no pre-written descriptions. LLM gets structured context and generates text live every time.
  2. Persistent but mutable — world persists like Minecraft, but things change over time (burnt pub gets rebuilt in 6 months).
  3. Context window budget — everything for a location must fit in a reasonable token budget.
- **Key reframe from Task 21:** Descriptions are NOT cached assets. The LLM generates fresh prose every visit. What persists is structured location data + state mutations + vocabulary tables. This aligns perfectly with the existing narrative engine spec (§4.4): "state → story text."
- **Design proposal — Location Record + Event-Sourced State:**
  - **LocationRecord** — the persistent structured data: name, type, biome, features, exits, contained entities/objects, architectural hints, atmosphere tags, generation seed. NOT prose — structured vocabulary for the LLM.
  - **World State Mutations** — append-only event log per location. Events: created, damaged, destroyed, rebuilt, modified, entity_arrived, entity_departed. Current state = fold over events.
  - **Recovery Rules** — declarative rules for world healing: building types have rebuild probability per game-month, biome regrowth rates, NPC repopulation triggers.
  - **LLM Context Assembly** — pipeline that builds the prompt from: location record (~200 tokens) + state overlay (~100) + player/party (~150) + quest context (~150) + nearby exits (~50) + time/weather (~30) + vocabulary hints (~50) = ~730 tokens input budget.
  - **Pixel Scene Decision** — LLM decides: use existing cached scene, modify existing scene, or generate new scene. Scene generation is a separate LLM call with pixel format reference.
  - **Three location source types (refined from Task 21):**
    - *Wilderness* — generated on first visit from biome + features + noise seed. LocationRecord persisted, prose generated live.
    - *Structures* — generated from building templates + settlement context. Richer LocationRecord with interior layout, objects, NPCs.
    - *Authored* — hand-crafted LocationRecords for key narrative locations. Same format, just hand-written instead of generated.
  - **Token budget breakdown for ~2K input context:**
    - System prompt + format rules: ~300 tokens
    - Location record: ~200 tokens
    - State overlay (current conditions, recent events): ~100 tokens
    - Player/party state: ~150 tokens
    - Active quest hooks: ~150 tokens
    - Exits + nearby locations summary: ~50 tokens
    - Time/weather/season: ~30 tokens
    - Vocabulary hints (biome descriptors): ~50 tokens
    - Remaining for pixel format ref (if generating scene): ~600 tokens (LLMD schema)
- **Open questions posed:**
  1. Event log retention — keep full history or compact after N game-months?
  2. Recovery rules scope for v1 — simple probability or complex faction/economy driven?
  3. Vocabulary tables format — flat key-value or hierarchical biome→feature→descriptor?
  4. Approval to write formal spec section
- **Files changed:** None (proposal only — awaiting stevenic approval)

## Task 23: Refined World Architecture — Algorithmic Generation + LLM Budgets

- **What:** Refined the world architecture based on stevenic's three budget/generation decisions. Separated algorithmic generation from LLM narration and pixel scene generation.
- **Context:** stevenic gave 3 key refinements to Task 22:
  1. Separate token budgets: text narration gets 16k-32k tokens, pixel generation gets up to 64k tokens
  2. Location generation must be algorithmic and free — archetype libraries (20 pub archetypes, etc.), not LLM calls
  3. Scene composition should have a simpler algorithm — template-based assembly where possible
- **Key design changes from Task 22:**
  - **Archetype Library Model** — `BuildingArchetype` type with name patterns + vocabulary, required/optional features, weighted object/NPC slots, exit templates, biome variants, scene templates. World engine picks archetype from seed, rolls concrete LocationRecord. Zero LLM cost.
  - **Expanded text budget (16-32k)** — system prompt (~2K), full LocationRecord (~500), event history chain (~1K), full player/party state (~1K), active quest chains (~2K), NPC profiles + dialogue history (~2K), nearby locations (~500), vocabulary/cultural context (~1K), conversation history (~4K). Much richer context than Task 22's ~1K estimate.
  - **Separate pixel budget (64k)** — independent LLM call, fires only on first visit or major state change. Cached afterward. Time-of-day/weather handled by renderer blend modes (free).
  - **Template-based scene assembly** — archetypes include scene templates with positioned slots (background, furniture, NPCs, lighting). World engine fills slots from asset library. LLM only generates missing sprites. Asset library grows over time → pixel LLM calls approach zero.
  - **Three-tier cost model:** algorithmic (free, every visit) → text LLM (16-32k, every visit) → pixel LLM (64k, first visit only)
- **Open questions posed:**
  1. Archetype library scope for v1 — how many per category?
  2. Scene template vs. full pixel LLM — pursue template assembly or keep 64k LLM as primary?
  3. Archetype file format — JSON data files or part of world gen spec?
  4. Ready to write formal spec section?
- **Files changed:** None (proposal only — awaiting stevenic decisions)

## Task 24: Architecture Decisions — Spec Amendment

- **What:** Amended `world_generation_spec.md` to formalize the three core randomness algorithms (OpenSimplex2, xorshift32 PRNG, FNV-1a hashing) and the pure functions architecture.
- **Context:** Beacon explained the architecture decisions to stevenic (OpenSimplex2 for smooth terrain noise, xorshift32 for discrete random choices, FNV-1a for positional seed derivation, pure functions for multiplayer determinism). stevenic approved and said "add it to the specs."
- **Spec changes:**
  - §2.1 — Updated seed-from-string method to specify FNV-1a explicitly
  - §2.2 — Expanded from generic "positional RNG" to full specification of FNV-1a hash + xorshift32 PRNG + RNG stream isolation pattern with salt constants. Includes rationale for algorithm choices and code examples.
  - §2.3 — Renamed to "Noise Functions — OpenSimplex2", added rationale for choosing OpenSimplex2 over Perlin/Simplex/OpenSimplex v1, added octave layer explanation and ridged noise definition
  - §2.4 — New "Algorithm Stack Summary" section with comparison table and data flow diagram
  - §12 — Expanded determinism guarantees: added RNG stream isolation (#3), pure functions (#6) with code example, World class cache exception note
  - §16.5 — Resolved open question (noise library choice → OpenSimplex2)
- **Files changed:** `world_generation_spec.md`
- **Handoff:** @beacon — implement the full architecture as discussed

## Task 25: Writer Teammate Proposal — "Bard"

- **What:** Analyzed stevenic's request to add a new writer teammate responsible for the base game's story. Proposed "Bard" (Narrative Designer) role with full gap analysis and boundary definitions.
- **Context:** stevenic identified that nobody owns the creative content layer — world lore, story arcs, factions, quest design, NPC profiles, archetype libraries. This gap becomes critical as we build out the text-first world architecture (Tasks 21-23).
- **Key analysis:**
  - Clear gap: Loremaster owns D&D rules/mechanics, Lexicon owns prompt architecture, nobody owns *what stories are told*
  - Proposed name: "Bard" (fits D&D theme, storyteller role)
  - Proposed ownership: `world/lore/**`, `world/quests/**`, `world/npcs/**`, `world/archetypes/**`, `world/vocabulary/**`
  - Key boundary: Bard writes *what* (content), Lexicon designs *how* (prompts), Loremaster ensures *mechanics* (rules), Scribe specs *format* (file structures)
  - Dependency flow: Specs (Scribe) → Lore & Story (Bard) → Rules (Loremaster) → Code (Beacon)
  - Noted: spec-first workflow applies — Scribe should spec lore file formats before Bard fills them in
  - This role unblocks archetype library work from Tasks 22-23
- **Status:** Awaiting stevenic approval before drafting SOUL.md
- **Files changed:** None (proposal only)

## Task 26: Created Bard Teammate

- **What:** Created the Bard (Narrative Designer) teammate directory and all required files. Updated team roster and shared docs.
- **Context:** stevenic approved the Bard proposal from Task 25 (said "yes" twice when asked to proceed).
- **Files created:**
  - `.teammates/bard/SOUL.md` — Full identity, principles, boundaries, ownership, capabilities
  - `.teammates/bard/WISDOM.md` — Empty template (no wisdom yet)
  - `.teammates/bard/memory/2026-03-26.md` — Onboarding log with key prior decisions that affect Bard's work
- **Files updated:**
  - `.teammates/README.md` — Added Bard to roster, dependency flow, and routing guide
  - `.teammates/CROSS-TEAM.md` — Added Bard to ownership scopes table
- **Key design decisions:**
  - Name: "Bard" — fits D&D theme, storyteller role
  - Ownership: `world/lore/**`, `world/factions/**`, `world/cultures/**`, `world/quests/**`, `world/npcs/**`, `world/archetypes/**`, `world/vocabulary/**`
  - Boundary: Bard writes *what* (content), Lexicon designs *how* (prompts), Loremaster ensures *mechanics* (rules), Scribe specs *format* (file structures)
  - Updated dependency flow: Specs (Scribe) → Lore & Story (Bard) → Rules (Loremaster) → Code (Beacon)
  - Bard's onboarding log captures all relevant prior architecture decisions (text-first world, archetype libraries, token budgets, event-sourced state)
- **Next steps:** Bard needs content schemas spec'd by Scribe before they can start filling in archetype libraries, vocabulary tables, etc. The spec-first workflow applies.

## Task 27: Narrative Engine Spec Amendment — §4.3, §4.4, §9

- **What:** Amended `loreweave_spec.md` to document the full DungeonMaster narrative engine architecture built by Beacon.
- **Context:** stevenic approved the proposal from the previous task. The existing spec sections were thin stubs (~30 lines) that didn't reflect the implemented architecture.
- **Spec changes:**
  - **§4.3 Intent Parser** — Replaced "Rule-based + LLM hybrid" with deterministic keyword parsing. Documented 10 action types with keyword tables, 8-directional matching with aliases, target/item substring resolution, stealth modifier, ParseResult type with failure hints.
  - **§4.4 Narrative Engine** — Expanded from 30 lines to 6 sub-sections:
    - §4.4.1 DM Turn Loop — full pipeline diagram, critical LLM constraint, turn seed derivation
    - §4.4.2 Action Resolution — routing, ActionResult type, immutable effect application
    - §4.4.3 Context Assembly — component table (location, entities, weather, events, exits, quests), all pure functions
    - §4.4.4 Prompt Architecture — system prompt (persona, constraints, cacheable) + user prompt (per-turn sections table)
    - §4.4.5 NarrativeAdapter Interface — pluggable LLM backend, model-agnostic design
    - §4.4.6 Configuration Defaults — 6 parameters with defaults and descriptions
  - **§9 Game Loop** — Replaced 12-line pseudocode with:
    - §9.1 Session Initialization — createSession() + describeSurroundings()
    - §9.2 Turn Processing — 10-step processTurn() pipeline
    - §9.3 Session Management — party, quest, entity, adapter management methods
- **All changes verified against implementation** in `packages/narrative/src/` (intent-parser.ts, dm.ts, action-resolver.ts, context-assembler.ts, prompt-builder.ts)
- **Files changed:** `loreweave_spec.md`

## Task 28: Update Bard Ownership Paths (world/ → worlds/*/)

- **What:** Updated `.teammates/CROSS-TEAM.md` and `.teammates/README.md` to reflect Bard's new multi-world folder structure.
- **Context:** Bard moved all content files from `world/` to `worlds/threadhallow/` to support multiple worlds. The shared docs still referenced the old `world/` paths.
- **Changes:**
  - CROSS-TEAM.md ownership table: `world/lore/**` → `worlds/*/lore/**` (and all other Bard paths). Added `worlds/*/regions/**`.
  - README.md roster: same path updates.
- **Files changed:** `.teammates/CROSS-TEAM.md`, `.teammates/README.md`

## Task 29: Created worlds/shared/ — Standard Archetype Library

- **What:** Created the `worlds/shared/` folder with standard archetype definitions for locations, NPCs, and vocabulary tables. Designed the archetype file format schema.
- **Context:** stevenic asked for a shared folder with standard definitions for various locations and NPCs that any world can draw from. This implements the archetype library concept from Tasks 22-23 (the "20 pub archetypes" idea).
- **Structure created:**
  ```
  worlds/shared/
  ├── README.md               # Overview and structure guide
  ├── archetype_format.md     # Schema definition for location + NPC archetypes
  ├── locations/
  │   ├── taverns/            # 3 variants: common_tavern, roadside_inn, rough_alehouse
  │   ├── shops/              # 3 variants: general_store, blacksmith, apothecary
  │   ├── temples/            # 1 variant: village_shrine
  │   ├── residences/         # 1 variant: cottage
  │   ├── civic/              # 1 variant: town_hall
  │   └── wilderness/         # 3 variants: forest_clearing, crossroads, cave_entrance
  ├── npcs/
  │   ├── merchants/          # 2 variants: shopkeeper, traveling_merchant
  │   ├── authority/          # 2 variants: village_elder, guard
  │   ├── craft/              # 1 variant: blacksmith
  │   ├── service/            # 2 variants: innkeeper, priest
  │   └── wanderers/          # 2 variants: hermit, ranger
  └── vocabulary/
      ├── atmosphere/         # biome_descriptors (7 biomes), time_of_day (8 periods)
      └── architecture/       # building_styles (7 biomes × 3 conditions)
  ```
- **Key design decisions:**
  - Archetype format uses probability tiers: `required` (100%), `common` (50-80%), `rare` (<20%) for features, objects, NPCs, exits
  - Location archetypes include: features, objects, NPCs, exits, atmosphere (sounds/smells/lighting), and layout
  - NPC archetypes include: traits, skills, inventory, knowledge, dialogue_hooks, voice_patterns, and schedule
  - Vocabulary tables organized by biome with weather modifiers for narrative engine context assembly
  - Separate from world-specific content — worlds/shared/ is generic, worlds/threadhallow/ has Threadhallow-specific lore
- **Files created:** 20 files total (2 docs + 12 location archetypes + 9 NPC archetypes + 3 vocabulary tables... actually 2 docs + 9 locations + 7 NPCs + 3 vocabulary = 21 files)
- **Files updated:** `.teammates/CROSS-TEAM.md` (added shared docs entry)
- **Next steps:** Bard should expand the archetype library with more variants per category (goal: ~20 per major category). The format schema could be formalized in a spec if needed.
